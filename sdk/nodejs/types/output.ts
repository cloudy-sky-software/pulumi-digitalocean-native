// *** WARNING: this file was generated by pulumigen. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as enums from "../types/enums";

import * as utilities from "../utilities";

export namespace account {
    export namespace v2 {
        export interface Account {
            /**
             * The total number of Droplets current user or team may have active at one time.
             */
            dropletLimit: number;
            /**
             * The email address used by the current user to register for DigitalOcean.
             */
            email: string;
            /**
             * If true, the user has verified their account via email. False otherwise.
             */
            emailVerified: boolean;
            /**
             * The total number of Floating IPs the current user or team may have.
             */
            floatingIpLimit: number;
            /**
             * The display name for the current user.
             */
            name?: string;
            /**
             * This value is one of "active", "warning" or "locked".
             */
            status: enums.account.v2.AccountStatus;
            /**
             * A human-readable message giving more details about the status of the account.
             */
            statusMessage: string;
            /**
             * When authorized in a team context, includes information about the current team.
             */
            team?: outputs.account.v2.AccountTeamProperties;
            /**
             * The unique universal identifier for the current user.
             */
            uuid: string;
        }
        /**
         * accountProvideDefaults sets the appropriate defaults for Account
         */
        export function accountProvideDefaults(val: Account): Account {
            return {
                ...val,
                emailVerified: (val.emailVerified) ?? false,
                status: (val.status) ?? "active",
            };
        }

        /**
         * When authorized in a team context, includes information about the current team.
         */
        export interface AccountTeamProperties {
            /**
             * The name for the current team.
             */
            name?: string;
            /**
             * The unique universal identifier for the current team.
             */
            uuid?: string;
        }

        export interface GetAccountProperties {
            account?: outputs.account.v2.Account;
        }
        /**
         * getAccountPropertiesProvideDefaults sets the appropriate defaults for GetAccountProperties
         */
        export function getAccountPropertiesProvideDefaults(val: GetAccountProperties): GetAccountProperties {
            return {
                ...val,
                account: (val.account ? outputs.account.v2.accountProvideDefaults(val.account) : undefined),
            };
        }

        export interface GetSshKeyProperties {
            sshKey?: outputs.account.v2.SshKeys;
        }

        export interface ListSshKeysItems {
            links?: outputs.account.v2.PageLinks;
            meta: outputs.account.v2.MetaMeta;
            sshKeys?: outputs.account.v2.SshKeys[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.account.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface SshKeys {
            /**
             * A unique identifier that differentiates this key from other keys using  a format that SSH recognizes. The fingerprint is created when the key is added to your account.
             */
            fingerprint?: string;
            /**
             * A unique identification number for this key. Can be used to embed a  specific SSH key into a Droplet.
             */
            id?: number;
            /**
             * A human-readable display name for this key, used to easily identify the SSH keys when they are displayed.
             */
            name: string;
            /**
             * The entire public key string that was uploaded. Embedded into the root user's `authorized_keys` file if you include this key during Droplet creation.
             */
            publicKey: string;
        }

    }
}

export namespace actions {
    export namespace v2 {
        export interface Action {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            region?: outputs.actions.v2.Region;
            regionSlug?: outputs.actions.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.actions.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * actionProvideDefaults sets the appropriate defaults for Action
         */
        export function actionProvideDefaults(val: Action): Action {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        export interface ActionRegionSlug {
        }

        export interface GetActionProperties {
            action?: outputs.actions.v2.Action;
        }
        /**
         * getActionPropertiesProvideDefaults sets the appropriate defaults for GetActionProperties
         */
        export function getActionPropertiesProvideDefaults(val: GetActionProperties): GetActionProperties {
            return {
                ...val,
                action: (val.action ? outputs.actions.v2.actionProvideDefaults(val.action) : undefined),
            };
        }

        export interface ListActionsItems {
            actions?: outputs.actions.v2.Action[];
            links?: outputs.actions.v2.PageLinks;
            meta: outputs.actions.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.actions.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

    }
}

export namespace apps {
    export namespace v2 {
        /**
         * An application's configuration and status.
         */
        export interface App {
            activeDeployment?: outputs.apps.v2.AppsDeployment;
            createdAt?: string;
            defaultIngress?: string;
            domains?: outputs.apps.v2.AppsDomain[];
            id?: string;
            inProgressDeployment?: outputs.apps.v2.AppsDeployment;
            lastDeploymentCreatedAt?: string;
            liveDomain?: string;
            liveUrl?: string;
            liveUrlBase?: string;
            ownerUuid?: string;
            pendingDeployment?: outputs.apps.v2.AppPendingDeployment;
            pinnedDeployment?: outputs.apps.v2.AppPinnedDeployment;
            projectId?: string;
            region?: outputs.apps.v2.AppsRegion;
            /**
             * The desired configuration of an application.
             */
            spec: outputs.apps.v2.AppSpec;
            tierSlug?: string;
            updatedAt?: string;
        }
        /**
         * appProvideDefaults sets the appropriate defaults for App
         */
        export function appProvideDefaults(val: App): App {
            return {
                ...val,
                activeDeployment: (val.activeDeployment ? outputs.apps.v2.appsDeploymentProvideDefaults(val.activeDeployment) : undefined),
                inProgressDeployment: (val.inProgressDeployment ? outputs.apps.v2.appsDeploymentProvideDefaults(val.inProgressDeployment) : undefined),
                pendingDeployment: (val.pendingDeployment ? outputs.apps.v2.appPendingDeploymentProvideDefaults(val.pendingDeployment) : undefined),
                pinnedDeployment: (val.pinnedDeployment ? outputs.apps.v2.appPinnedDeploymentProvideDefaults(val.pinnedDeployment) : undefined),
            };
        }

        export interface AppAlert {
            componentName?: string;
            emails?: string[];
            id?: string;
            phase?: enums.apps.v2.AppAlertPhase;
            progress?: outputs.apps.v2.AppAlertProgress;
            slackWebhooks?: outputs.apps.v2.AppAlertSlackWebhook[];
            spec?: outputs.apps.v2.AppAlertSpec;
        }
        /**
         * appAlertProvideDefaults sets the appropriate defaults for AppAlert
         */
        export function appAlertProvideDefaults(val: AppAlert): AppAlert {
            return {
                ...val,
                phase: (val.phase) ?? "UNKNOWN",
                spec: (val.spec ? outputs.apps.v2.appAlertSpecProvideDefaults(val.spec) : undefined),
            };
        }

        export interface AppAlertProgress {
            steps?: outputs.apps.v2.AppAlertProgressStep[];
        }

        export interface AppAlertProgressStep {
            endedAt?: string;
            name?: string;
            reason?: outputs.apps.v2.AppAlertProgressStepReason;
            startedAt?: string;
            status?: enums.apps.v2.AppAlertProgressStepStatus;
        }
        /**
         * appAlertProgressStepProvideDefaults sets the appropriate defaults for AppAlertProgressStep
         */
        export function appAlertProgressStepProvideDefaults(val: AppAlertProgressStep): AppAlertProgressStep {
            return {
                ...val,
                status: (val.status) ?? "UNKNOWN",
            };
        }

        export interface AppAlertProgressStepReason {
            code?: string;
            message?: string;
        }

        export interface AppAlertSlackWebhook {
            channel?: string;
            url?: string;
        }

        export interface AppAlertSpec {
            /**
             * Is the alert disabled?
             */
            disabled?: boolean;
            operator?: enums.apps.v2.AppsValidateAppSpecAppAlertSpecOperator;
            rule?: enums.apps.v2.AppsValidateAppSpecAppAlertSpecRule;
            /**
             * Threshold value for alert
             */
            value?: number;
            window?: enums.apps.v2.AppsValidateAppSpecAppAlertSpecWindow;
        }
        /**
         * appAlertSpecProvideDefaults sets the appropriate defaults for AppAlertSpec
         */
        export function appAlertSpecProvideDefaults(val: AppAlertSpec): AppAlertSpec {
            return {
                ...val,
                operator: (val.operator) ?? "UNSPECIFIED_OPERATOR",
                rule: (val.rule) ?? "UNSPECIFIED_RULE",
                window: (val.window) ?? "UNSPECIFIED_WINDOW",
            };
        }

        export interface AppDatabaseSpec {
            /**
             * The name of the underlying DigitalOcean DBaaS cluster. This is required for production databases. For dev databases, if cluster_name is not set, a new cluster will be provisioned.
             */
            clusterName?: string;
            /**
             * The name of the MySQL or PostgreSQL database to configure.
             */
            dbName?: string;
            /**
             * The name of the MySQL or PostgreSQL user to configure.
             */
            dbUser?: string;
            /**
             * - MYSQL: MySQL
             * - PG: PostgreSQL
             * - REDIS: Redis
             */
            engine?: enums.apps.v2.AppsValidateAppSpecAppDatabaseSpecEngine;
            /**
             * The name. Must be unique across all components within the same app.
             */
            name: string;
            /**
             * Whether this is a production or dev database.
             */
            production?: boolean;
            /**
             * The version of the database engine
             */
            version?: string;
        }
        /**
         * appDatabaseSpecProvideDefaults sets the appropriate defaults for AppDatabaseSpec
         */
        export function appDatabaseSpecProvideDefaults(val: AppDatabaseSpec): AppDatabaseSpec {
            return {
                ...val,
                engine: (val.engine) ?? "UNSET",
            };
        }

        export interface AppDomainSpec {
            /**
             * The hostname for the domain
             */
            domain: string;
            /**
             * The minimum version of TLS a client application can use to access resources for the domain.  Must be one of the following values wrapped within quotations: `"1.2"` or `"1.3"`.
             */
            minimumTlsVersion?: enums.apps.v2.AppsValidateAppSpecAppDomainSpecMinimumTlsVersion;
            /**
             * - DEFAULT: The default `.ondigitalocean.app` domain assigned to this app
             * - PRIMARY: The primary domain for this app that is displayed as the default in the control panel, used in bindable environment variables, and any other places that reference an app's live URL. Only one domain may be set as primary.
             * - ALIAS: A non-primary domain
             */
            type?: enums.apps.v2.AppsValidateAppSpecAppDomainSpecType;
            /**
             * Indicates whether the domain includes all sub-domains, in addition to the given domain
             */
            wildcard?: boolean;
            /**
             * Optional. If the domain uses DigitalOcean DNS and you would like App
             * Platform to automatically manage it for you, set this to the name of the
             * domain on your account.
             *
             * For example, If the domain you are adding is `app.domain.com`, the zone
             * could be `domain.com`.
             */
            zone?: string;
        }
        /**
         * appDomainSpecProvideDefaults sets the appropriate defaults for AppDomainSpec
         */
        export function appDomainSpecProvideDefaults(val: AppDomainSpec): AppDomainSpec {
            return {
                ...val,
                type: (val.type) ?? "UNSPECIFIED",
            };
        }

        export interface AppDomainValidation {
            txtName?: string;
            txtValue?: string;
        }

        export interface AppFunctionsSpec {
            alerts?: outputs.apps.v2.AppAlertSpec[];
            cors?: outputs.apps.v2.AppsCorsPolicy;
            /**
             * A list of environment variables made available to the component.
             */
            envs?: outputs.apps.v2.AppVariableDefinition[];
            git?: outputs.apps.v2.AppsGitSourceSpec;
            github?: outputs.apps.v2.AppsGithubSourceSpec;
            gitlab?: outputs.apps.v2.AppsGitlabSourceSpec;
            logDestinations?: outputs.apps.v2.AppLogDestinationDefinition;
            /**
             * The name. Must be unique across all components within the same app.
             */
            name: string;
            /**
             * A list of HTTP routes that should be routed to this component.
             */
            routes?: outputs.apps.v2.AppRouteSpec[];
            /**
             * An optional path to the working directory to use for the build. For Dockerfile builds, this will be used as the build context. Must be relative to the root of the repo.
             */
            sourceDir?: string;
        }

        /**
         * Specification for app ingress configurations.
         */
        export interface AppIngressSpec {
            /**
             * Rules for configuring HTTP ingress for component routes, CORS, rewrites, and redirects.
             */
            rules?: outputs.apps.v2.AppIngressSpecRule[];
        }

        export interface AppIngressSpecRule {
            /**
             * The component to route to. Only one of `component` or `redirect` may be set.
             */
            component?: outputs.apps.v2.AppIngressSpecRuleRoutingComponent;
            cors?: outputs.apps.v2.AppsCorsPolicy;
            /**
             * The match configuration for the rule.
             */
            match?: outputs.apps.v2.AppIngressSpecRuleMatch;
            /**
             * The redirect configuration for the rule. Only one of `component` or `redirect` may be set.
             */
            redirect?: outputs.apps.v2.AppIngressSpecRuleRoutingRedirect;
        }

        /**
         * The match configuration for the rule.
         */
        export interface AppIngressSpecRuleMatch {
            /**
             * The path to match on.
             */
            path: outputs.apps.v2.AppIngressSpecRuleStringMatch;
        }

        /**
         * The component to route to. Only one of `component` or `redirect` may be set.
         */
        export interface AppIngressSpecRuleRoutingComponent {
            /**
             * The name of the component to route to.
             */
            name: string;
            /**
             * An optional flag to preserve the path that is forwarded to the backend service. By default, the HTTP request path will be trimmed from the left when forwarded to the component. For example, a component with `path=/api` will have requests to `/api/list` trimmed to `/list`. If this value is `true`, the path will remain `/api/list`. Note: this is not applicable for Functions Components and is mutually exclusive with `rewrite`.
             */
            preservePathPrefix?: string;
            /**
             * An optional field that will rewrite the path of the component to be what is specified here. By default, the HTTP request path will be trimmed from the left when forwarded to the component. For example, a component with `path=/api` will have requests to `/api/list` trimmed to `/list`. If you specified the rewrite to be `/v1/`, requests to `/api/list` would be rewritten to `/v1/list`. Note: this is mutually exclusive with `preserve_path_prefix`.
             */
            rewrite?: string;
        }

        /**
         * The redirect configuration for the rule. Only one of `component` or `redirect` may be set.
         */
        export interface AppIngressSpecRuleRoutingRedirect {
            /**
             * The authority/host to redirect to. This can be a hostname or IP address. Note: use `port` to set the port.
             */
            authority?: string;
            /**
             * The port to redirect to.
             */
            port?: number;
            /**
             * The redirect code to use. Defaults to `302`. Supported values are 300, 301, 302, 303, 304, 307, 308.
             */
            redirectCode?: number;
            /**
             * The scheme to redirect to. Supported values are `http` or `https`. Default: `https`.
             */
            scheme?: string;
            /**
             * An optional URI path to redirect to. Note: if this is specified the whole URI of the original request will be overwritten to this value, irrespective of the original request URI being matched.
             */
            uri?: string;
        }

        /**
         * The path to match on.
         */
        export interface AppIngressSpecRuleStringMatch {
            /**
             * Prefix-based match. For example, `/api` will match `/api`, `/api/`, and any nested paths such as `/api/v1/endpoint`.
             */
            prefix: string;
        }

        export interface AppJobSpec {
            /**
             * An optional build command to run while building this component from source.
             */
            buildCommand?: string;
            /**
             * The path to the Dockerfile relative to the root of the repo. If set, it will be used to build this component. Otherwise, App Platform will attempt to build it using buildpacks.
             */
            dockerfilePath?: string;
            /**
             * An environment slug describing the type of this app. For a full list, please refer to [the product documentation](https://www.digitalocean.com/docs/app-platform/).
             */
            environmentSlug?: string;
            /**
             * A list of environment variables made available to the component.
             */
            envs?: outputs.apps.v2.AppVariableDefinition[];
            git?: outputs.apps.v2.AppsGitSourceSpec;
            github?: outputs.apps.v2.AppsGithubSourceSpec;
            gitlab?: outputs.apps.v2.AppsGitlabSourceSpec;
            image?: outputs.apps.v2.AppsImageSourceSpec;
            /**
             * The amount of instances that this component should be scaled to. Default: 1
             */
            instanceCount?: number;
            /**
             * The instance size to use for this component. Default: `basic-xxs`
             */
            instanceSizeSlug?: enums.apps.v2.AppsValidateAppSpecAppComponentInstanceBaseInstanceSizeSlug;
            /**
             * - UNSPECIFIED: Default job type, will auto-complete to POST_DEPLOY kind.
             * - PRE_DEPLOY: Indicates a job that runs before an app deployment.
             * - POST_DEPLOY: Indicates a job that runs after an app deployment.
             * - FAILED_DEPLOY: Indicates a job that runs after a component fails to deploy.
             */
            kind?: enums.apps.v2.AppsValidateAppSpecAppJobSpecPropertiesKind;
            logDestinations?: outputs.apps.v2.AppLogDestinationDefinition;
            /**
             * The name. Must be unique across all components within the same app.
             */
            name?: string;
            /**
             * An optional run command to override the component's default.
             */
            runCommand?: string;
            /**
             * An optional path to the working directory to use for the build. For Dockerfile builds, this will be used as the build context. Must be relative to the root of the repo.
             */
            sourceDir?: string;
        }
        /**
         * appJobSpecProvideDefaults sets the appropriate defaults for AppJobSpec
         */
        export function appJobSpecProvideDefaults(val: AppJobSpec): AppJobSpec {
            return {
                ...val,
                image: (val.image ? outputs.apps.v2.appsImageSourceSpecProvideDefaults(val.image) : undefined),
                instanceCount: (val.instanceCount) ?? 1,
                instanceSizeSlug: (val.instanceSizeSlug) ?? "basic-xxs",
                kind: (val.kind) ?? "UNSPECIFIED",
            };
        }

        /**
         * DataDog configuration.
         */
        export interface AppLogDestinationDatadogSpec {
            /**
             * Datadog API key.
             */
            apiKey: string;
            /**
             * Datadog HTTP log intake endpoint.
             */
            endpoint?: string;
        }

        export interface AppLogDestinationDefinition {
            /**
             * DataDog configuration.
             */
            datadog?: outputs.apps.v2.AppLogDestinationDatadogSpec;
            /**
             * Logtail configuration.
             */
            logtail?: outputs.apps.v2.AppLogDestinationLogtailSpec;
            name: string;
            /**
             * Papertrail configuration.
             */
            papertrail?: outputs.apps.v2.AppLogDestinationPapertrailSpec;
        }

        /**
         * Logtail configuration.
         */
        export interface AppLogDestinationLogtailSpec {
            /**
             * Logtail token.
             */
            token?: string;
        }

        /**
         * Papertrail configuration.
         */
        export interface AppLogDestinationPapertrailSpec {
            /**
             * Papertrail syslog endpoint.
             */
            endpoint: string;
        }

        export interface AppMetricsBandwidthUsage {
            /**
             * A list of bandwidth usage details by app.
             */
            appBandwidthUsage?: outputs.apps.v2.AppMetricsBandwidthUsageDetails[];
            /**
             * The date for the metrics data.
             */
            date?: string;
        }

        /**
         * Bandwidth usage for an app.
         */
        export interface AppMetricsBandwidthUsageDetails {
            /**
             * The ID of the app.
             */
            appId?: string;
            /**
             * The used bandwidth amount in bytes.
             */
            bandwidthBytes?: string;
        }

        export interface AppPendingDeployment {
            cause?: string;
            clonedFrom?: string;
            createdAt?: string;
            functions?: outputs.apps.v2.AppsDeploymentFunctions[];
            id?: string;
            jobs?: outputs.apps.v2.AppsDeploymentJob[];
            phase?: enums.apps.v2.AppsDeploymentPhase;
            phaseLastUpdatedAt?: string;
            progress?: outputs.apps.v2.AppsDeploymentProgress;
            services?: outputs.apps.v2.AppsDeploymentService[];
            /**
             * The desired configuration of an application.
             */
            spec?: outputs.apps.v2.AppSpec;
            staticSites?: outputs.apps.v2.AppsDeploymentStaticSite[];
            tierSlug?: string;
            updatedAt?: string;
            workers?: outputs.apps.v2.AppsDeploymentWorker[];
        }
        /**
         * appPendingDeploymentProvideDefaults sets the appropriate defaults for AppPendingDeployment
         */
        export function appPendingDeploymentProvideDefaults(val: AppPendingDeployment): AppPendingDeployment {
            return {
                ...val,
                phase: (val.phase) ?? "UNKNOWN",
            };
        }

        export interface AppPinnedDeployment {
            cause?: string;
            clonedFrom?: string;
            createdAt?: string;
            functions?: outputs.apps.v2.AppsDeploymentFunctions[];
            id?: string;
            jobs?: outputs.apps.v2.AppsDeploymentJob[];
            phase?: enums.apps.v2.AppsDeploymentPhase;
            phaseLastUpdatedAt?: string;
            progress?: outputs.apps.v2.AppsDeploymentProgress;
            services?: outputs.apps.v2.AppsDeploymentService[];
            /**
             * The desired configuration of an application.
             */
            spec?: outputs.apps.v2.AppSpec;
            staticSites?: outputs.apps.v2.AppsDeploymentStaticSite[];
            tierSlug?: string;
            updatedAt?: string;
            workers?: outputs.apps.v2.AppsDeploymentWorker[];
        }
        /**
         * appPinnedDeploymentProvideDefaults sets the appropriate defaults for AppPinnedDeployment
         */
        export function appPinnedDeploymentProvideDefaults(val: AppPinnedDeployment): AppPinnedDeployment {
            return {
                ...val,
                phase: (val.phase) ?? "UNKNOWN",
            };
        }

        export interface AppResponse {
            /**
             * An application's configuration and status.
             */
            app?: outputs.apps.v2.App;
        }
        /**
         * appResponseProvideDefaults sets the appropriate defaults for AppResponse
         */
        export function appResponseProvideDefaults(val: AppResponse): AppResponse {
            return {
                ...val,
                app: (val.app ? outputs.apps.v2.appProvideDefaults(val.app) : undefined),
            };
        }

        export interface AppRollbackValidationCondition {
            /**
             * A code identifier that represents the failing condition.
             *
             * Failing conditions:
             *   - `incompatible_phase` - indicates that the deployment's phase is not suitable for rollback.
             *   - `incompatible_result` - indicates that the deployment's result is not suitable for rollback.
             *   - `exceeded_revision_limit` - indicates that the app has exceeded the rollback revision limits for its tier.
             *   - `app_pinned` - indicates that there is already a rollback in progress and the app is pinned.
             *   - `database_config_conflict` - indicates that the deployment's database config is different than the current config.
             *   - `region_conflict` - indicates that the deployment's region differs from the current app region.
             *   
             * Warning conditions:
             *   - `static_site_requires_rebuild` - indicates that the deployment contains at least one static site that will require a rebuild.
             *   - `image_source_missing_digest` - indicates that the deployment contains at least one component with an image source that is missing a digest.
             */
            code?: enums.apps.v2.AppsValidateRollbackAppRollbackValidationConditionCode;
            components?: string[];
            /**
             * A human-readable message describing the failing condition.
             */
            message?: string;
        }

        export interface AppRouteSpec {
            /**
             * An HTTP path prefix. Paths must start with / and must be unique across all components within an app.
             */
            path?: string;
            /**
             * An optional flag to preserve the path that is forwarded to the backend service. By default, the HTTP request path will be trimmed from the left when forwarded to the component. For example, a component with `path=/api` will have requests to `/api/list` trimmed to `/list`. If this value is `true`, the path will remain `/api/list`.
             */
            preservePathPrefix?: boolean;
        }

        export interface AppServiceSpec {
            /**
             * An optional build command to run while building this component from source.
             */
            buildCommand?: string;
            cors?: outputs.apps.v2.AppsCorsPolicy;
            /**
             * The path to the Dockerfile relative to the root of the repo. If set, it will be used to build this component. Otherwise, App Platform will attempt to build it using buildpacks.
             */
            dockerfilePath?: string;
            /**
             * An environment slug describing the type of this app. For a full list, please refer to [the product documentation](https://www.digitalocean.com/docs/app-platform/).
             */
            environmentSlug?: string;
            /**
             * A list of environment variables made available to the component.
             */
            envs?: outputs.apps.v2.AppVariableDefinition[];
            git?: outputs.apps.v2.AppsGitSourceSpec;
            github?: outputs.apps.v2.AppsGithubSourceSpec;
            gitlab?: outputs.apps.v2.AppsGitlabSourceSpec;
            healthCheck?: outputs.apps.v2.AppServiceSpecHealthCheck;
            /**
             * The internal port on which this service's run command will listen. Default: 8080
             * If there is not an environment variable with the name `PORT`, one will be automatically added with its value set to the value of this field.
             */
            httpPort?: number;
            image?: outputs.apps.v2.AppsImageSourceSpec;
            /**
             * The amount of instances that this component should be scaled to. Default: 1
             */
            instanceCount?: number;
            /**
             * The instance size to use for this component. Default: `basic-xxs`
             */
            instanceSizeSlug?: enums.apps.v2.AppsValidateAppSpecAppComponentInstanceBaseInstanceSizeSlug;
            /**
             * The ports on which this service will listen for internal traffic.
             */
            internalPorts?: number[];
            logDestinations?: outputs.apps.v2.AppLogDestinationDefinition;
            /**
             * The name. Must be unique across all components within the same app.
             */
            name?: string;
            /**
             * A list of HTTP routes that should be routed to this component.
             */
            routes?: outputs.apps.v2.AppRouteSpec[];
            /**
             * An optional run command to override the component's default.
             */
            runCommand?: string;
            /**
             * An optional path to the working directory to use for the build. For Dockerfile builds, this will be used as the build context. Must be relative to the root of the repo.
             */
            sourceDir?: string;
        }
        /**
         * appServiceSpecProvideDefaults sets the appropriate defaults for AppServiceSpec
         */
        export function appServiceSpecProvideDefaults(val: AppServiceSpec): AppServiceSpec {
            return {
                ...val,
                image: (val.image ? outputs.apps.v2.appsImageSourceSpecProvideDefaults(val.image) : undefined),
                instanceCount: (val.instanceCount) ?? 1,
                instanceSizeSlug: (val.instanceSizeSlug) ?? "basic-xxs",
            };
        }

        export interface AppServiceSpecHealthCheck {
            /**
             * The number of failed health checks before considered unhealthy.
             */
            failureThreshold?: number;
            /**
             * The route path used for the HTTP health check ping. If not set, the HTTP health check will be disabled and a TCP health check used instead.
             */
            httpPath?: string;
            /**
             * The number of seconds to wait before beginning health checks.
             */
            initialDelaySeconds?: number;
            /**
             * The number of seconds to wait between health checks.
             */
            periodSeconds?: number;
            /**
             * The port on which the health check will be performed. If not set, the health check will be performed on the component's http_port.
             */
            port?: number;
            /**
             * The number of successful health checks before considered healthy.
             */
            successThreshold?: number;
            /**
             * The number of seconds after which the check times out.
             */
            timeoutSeconds?: number;
        }

        /**
         * The desired configuration of an application.
         */
        export interface AppSpec {
            /**
             * Database instances which can provide persistence to workloads within the
             * application.
             */
            databases?: outputs.apps.v2.AppDatabaseSpec[];
            /**
             * A set of hostnames where the application will be available.
             */
            domains?: outputs.apps.v2.AppDomainSpec[];
            /**
             * Workloads which expose publicly-accessible HTTP services via Functions Components.
             */
            functions?: outputs.apps.v2.AppFunctionsSpec[];
            /**
             * Specification for app ingress configurations.
             */
            ingress?: outputs.apps.v2.AppIngressSpec;
            /**
             * Pre and post deployment workloads which do not expose publicly-accessible HTTP routes.
             */
            jobs?: outputs.apps.v2.AppJobSpec[];
            /**
             * The name of the app. Must be unique across all apps in the same account.
             */
            name: string;
            /**
             * The slug form of the geographical origin of the app. Default: `nearest available`
             */
            region?: enums.apps.v2.AppsValidateAppSpecAppSpecRegion;
            /**
             * Workloads which expose publicly-accessible HTTP services.
             */
            services?: outputs.apps.v2.AppServiceSpec[];
            /**
             * Content which can be rendered to static web assets.
             */
            staticSites?: outputs.apps.v2.AppStaticSiteSpec[];
            /**
             * Workloads which do not expose publicly-accessible HTTP services.
             */
            workers?: outputs.apps.v2.AppWorkerSpec[];
        }

        export interface AppStaticSiteSpec {
            /**
             * An optional build command to run while building this component from source.
             */
            buildCommand?: string;
            /**
             * The name of the document to use as the fallback for any requests to documents that are not found when serving this static site. Only 1 of `catchall_document` or `error_document` can be set.
             */
            catchallDocument?: string;
            cors?: outputs.apps.v2.AppsCorsPolicy;
            /**
             * The path to the Dockerfile relative to the root of the repo. If set, it will be used to build this component. Otherwise, App Platform will attempt to build it using buildpacks.
             */
            dockerfilePath?: string;
            /**
             * An environment slug describing the type of this app. For a full list, please refer to [the product documentation](https://www.digitalocean.com/docs/app-platform/).
             */
            environmentSlug?: string;
            /**
             * A list of environment variables made available to the component.
             */
            envs?: outputs.apps.v2.AppVariableDefinition[];
            /**
             * The name of the error document to use when serving this static site. Default: 404.html. If no such file exists within the built assets, App Platform will supply one.
             */
            errorDocument?: string;
            git?: outputs.apps.v2.AppsGitSourceSpec;
            github?: outputs.apps.v2.AppsGithubSourceSpec;
            gitlab?: outputs.apps.v2.AppsGitlabSourceSpec;
            image?: outputs.apps.v2.AppsImageSourceSpec;
            /**
             * The name of the index document to use when serving this static site. Default: index.html
             */
            indexDocument?: string;
            logDestinations?: outputs.apps.v2.AppLogDestinationDefinition;
            /**
             * The name. Must be unique across all components within the same app.
             */
            name?: string;
            /**
             * An optional path to where the built assets will be located, relative to the build context. If not set, App Platform will automatically scan for these directory names: `_static`, `dist`, `public`, `build`.
             */
            outputDir?: string;
            /**
             * A list of HTTP routes that should be routed to this component.
             */
            routes?: outputs.apps.v2.AppRouteSpec[];
            /**
             * An optional run command to override the component's default.
             */
            runCommand?: string;
            /**
             * An optional path to the working directory to use for the build. For Dockerfile builds, this will be used as the build context. Must be relative to the root of the repo.
             */
            sourceDir?: string;
        }
        /**
         * appStaticSiteSpecProvideDefaults sets the appropriate defaults for AppStaticSiteSpec
         */
        export function appStaticSiteSpecProvideDefaults(val: AppStaticSiteSpec): AppStaticSiteSpec {
            return {
                ...val,
                errorDocument: (val.errorDocument) ?? "404.html",
                image: (val.image ? outputs.apps.v2.appsImageSourceSpecProvideDefaults(val.image) : undefined),
                indexDocument: (val.indexDocument) ?? "index.html",
            };
        }

        export interface AppVariableDefinition {
            /**
             * The variable name
             */
            key: string;
            /**
             * - RUN_TIME: Made available only at run-time
             * - BUILD_TIME: Made available only at build-time
             * - RUN_AND_BUILD_TIME: Made available at both build and run-time
             */
            scope?: enums.apps.v2.AppsValidateAppSpecAppVariableDefinitionScope;
            /**
             * - GENERAL: A plain-text environment variable
             * - SECRET: A secret encrypted environment variable
             */
            type?: enums.apps.v2.AppsValidateAppSpecAppVariableDefinitionType;
            /**
             * The value. If the type is `SECRET`, the value will be encrypted on first submission. On following submissions, the encrypted value should be used.
             */
            value?: string;
        }
        /**
         * appVariableDefinitionProvideDefaults sets the appropriate defaults for AppVariableDefinition
         */
        export function appVariableDefinitionProvideDefaults(val: AppVariableDefinition): AppVariableDefinition {
            return {
                ...val,
                scope: (val.scope) ?? "RUN_AND_BUILD_TIME",
                type: (val.type) ?? "GENERAL",
            };
        }

        export interface AppWorkerSpec {
            /**
             * An optional build command to run while building this component from source.
             */
            buildCommand?: string;
            /**
             * The path to the Dockerfile relative to the root of the repo. If set, it will be used to build this component. Otherwise, App Platform will attempt to build it using buildpacks.
             */
            dockerfilePath?: string;
            /**
             * An environment slug describing the type of this app. For a full list, please refer to [the product documentation](https://www.digitalocean.com/docs/app-platform/).
             */
            environmentSlug?: string;
            /**
             * A list of environment variables made available to the component.
             */
            envs?: outputs.apps.v2.AppVariableDefinition[];
            git?: outputs.apps.v2.AppsGitSourceSpec;
            github?: outputs.apps.v2.AppsGithubSourceSpec;
            gitlab?: outputs.apps.v2.AppsGitlabSourceSpec;
            image?: outputs.apps.v2.AppsImageSourceSpec;
            /**
             * The amount of instances that this component should be scaled to. Default: 1
             */
            instanceCount?: number;
            /**
             * The instance size to use for this component. Default: `basic-xxs`
             */
            instanceSizeSlug?: enums.apps.v2.AppsValidateAppSpecAppComponentInstanceBaseInstanceSizeSlug;
            logDestinations?: outputs.apps.v2.AppLogDestinationDefinition;
            /**
             * The name. Must be unique across all components within the same app.
             */
            name?: string;
            /**
             * An optional run command to override the component's default.
             */
            runCommand?: string;
            /**
             * An optional path to the working directory to use for the build. For Dockerfile builds, this will be used as the build context. Must be relative to the root of the repo.
             */
            sourceDir?: string;
        }
        /**
         * appWorkerSpecProvideDefaults sets the appropriate defaults for AppWorkerSpec
         */
        export function appWorkerSpecProvideDefaults(val: AppWorkerSpec): AppWorkerSpec {
            return {
                ...val,
                image: (val.image ? outputs.apps.v2.appsImageSourceSpecProvideDefaults(val.image) : undefined),
                instanceCount: (val.instanceCount) ?? 1,
                instanceSizeSlug: (val.instanceSizeSlug) ?? "basic-xxs",
            };
        }

        export interface AppsCorsPolicy {
            /**
             * Whether browsers should expose the response to the client-side JavaScript code when the request’s credentials mode is include. This configures the `Access-Control-Allow-Credentials` header.
             */
            allowCredentials?: boolean;
            /**
             * The set of allowed HTTP request headers. This configures the `Access-Control-Allow-Headers` header.
             */
            allowHeaders?: string[];
            /**
             * The set of allowed HTTP methods. This configures the `Access-Control-Allow-Methods` header.
             */
            allowMethods?: string[];
            /**
             * The set of allowed CORS origins.
             */
            allowOrigins?: outputs.apps.v2.AppsStringMatch[];
            /**
             * The set of HTTP response headers that browsers are allowed to access. This configures the `Access-Control-Expose-Headers` header.
             */
            exposeHeaders?: string[];
            /**
             * An optional duration specifying how long browsers can cache the results of a preflight request. This configures the `Access-Control-Max-Age` header.
             */
            maxAge?: string;
        }

        export interface AppsDeployment {
            cause?: string;
            clonedFrom?: string;
            createdAt?: string;
            functions?: outputs.apps.v2.AppsDeploymentFunctions[];
            id?: string;
            jobs?: outputs.apps.v2.AppsDeploymentJob[];
            phase?: enums.apps.v2.AppsDeploymentPhase;
            phaseLastUpdatedAt?: string;
            progress?: outputs.apps.v2.AppsDeploymentProgress;
            services?: outputs.apps.v2.AppsDeploymentService[];
            /**
             * The desired configuration of an application.
             */
            spec?: outputs.apps.v2.AppSpec;
            staticSites?: outputs.apps.v2.AppsDeploymentStaticSite[];
            tierSlug?: string;
            updatedAt?: string;
            workers?: outputs.apps.v2.AppsDeploymentWorker[];
        }
        /**
         * appsDeploymentProvideDefaults sets the appropriate defaults for AppsDeployment
         */
        export function appsDeploymentProvideDefaults(val: AppsDeployment): AppsDeployment {
            return {
                ...val,
                phase: (val.phase) ?? "UNKNOWN",
            };
        }

        export interface AppsDeploymentFunctions {
            name?: string;
            /**
             * The namespace where the functions are deployed.
             */
            namespace?: string;
            /**
             * The commit hash of the repository that was used to build this functions component.
             */
            sourceCommitHash?: string;
        }

        export interface AppsDeploymentJob {
            name?: string;
            sourceCommitHash?: string;
        }

        export interface AppsDeploymentProgress {
            errorSteps?: number;
            pendingSteps?: number;
            runningSteps?: number;
            steps?: outputs.apps.v2.AppsDeploymentProgressStep[];
            successSteps?: number;
            summarySteps?: outputs.apps.v2.AppsDeploymentProgressStep[];
            totalSteps?: number;
        }

        export interface AppsDeploymentProgressStep {
            componentName?: string;
            endedAt?: string;
            /**
             * The base of a human-readable description of the step intended to be combined with the component name for presentation. For example:
             *
             * `message_base` = "Building service"
             * `component_name` = "api"
             */
            messageBase?: string;
            name?: string;
            reason?: outputs.apps.v2.AppsDeploymentProgressStepReason;
            startedAt?: string;
            status?: enums.apps.v2.AppsDeploymentProgressStepStatus;
            steps?: any[];
        }
        /**
         * appsDeploymentProgressStepProvideDefaults sets the appropriate defaults for AppsDeploymentProgressStep
         */
        export function appsDeploymentProgressStepProvideDefaults(val: AppsDeploymentProgressStep): AppsDeploymentProgressStep {
            return {
                ...val,
                status: (val.status) ?? "UNKNOWN",
            };
        }

        export interface AppsDeploymentProgressStepReason {
            code?: string;
            message?: string;
        }

        export interface AppsDeploymentResponse {
            deployment?: outputs.apps.v2.AppsDeployment;
        }
        /**
         * appsDeploymentResponseProvideDefaults sets the appropriate defaults for AppsDeploymentResponse
         */
        export function appsDeploymentResponseProvideDefaults(val: AppsDeploymentResponse): AppsDeploymentResponse {
            return {
                ...val,
                deployment: (val.deployment ? outputs.apps.v2.appsDeploymentProvideDefaults(val.deployment) : undefined),
            };
        }

        export interface AppsDeploymentService {
            name?: string;
            sourceCommitHash?: string;
        }

        export interface AppsDeploymentStaticSite {
            name?: string;
            sourceCommitHash?: string;
        }

        export interface AppsDeploymentWorker {
            name?: string;
            sourceCommitHash?: string;
        }

        export interface AppsDeploymentsResponse {
            deployments?: outputs.apps.v2.AppsDeployment[];
            links?: outputs.apps.v2.PageLinks;
            meta: outputs.apps.v2.MetaMeta;
        }

        export interface AppsDomain {
            certificateExpiresAt?: string;
            id?: string;
            phase?: enums.apps.v2.AppsDomainPhase;
            progress?: outputs.apps.v2.AppsDomainProgress;
            rotateValidationRecords?: boolean;
            spec?: outputs.apps.v2.AppDomainSpec;
            validations?: outputs.apps.v2.AppDomainValidation[];
        }
        /**
         * appsDomainProvideDefaults sets the appropriate defaults for AppsDomain
         */
        export function appsDomainProvideDefaults(val: AppsDomain): AppsDomain {
            return {
                ...val,
                phase: (val.phase) ?? "UNKNOWN",
                spec: (val.spec ? outputs.apps.v2.appDomainSpecProvideDefaults(val.spec) : undefined),
            };
        }

        export interface AppsDomainProgress {
            steps?: any[];
        }

        export interface AppsGetInstanceSizeResponse {
            instanceSize?: outputs.apps.v2.AppsInstanceSize;
        }
        /**
         * appsGetInstanceSizeResponseProvideDefaults sets the appropriate defaults for AppsGetInstanceSizeResponse
         */
        export function appsGetInstanceSizeResponseProvideDefaults(val: AppsGetInstanceSizeResponse): AppsGetInstanceSizeResponse {
            return {
                ...val,
                instanceSize: (val.instanceSize ? outputs.apps.v2.appsInstanceSizeProvideDefaults(val.instanceSize) : undefined),
            };
        }

        export interface AppsGetLogsResponse {
            historicUrls?: string[];
            /**
             * A URL of the real-time live logs. This URL may use either the `https://` or `wss://` protocols and will keep pushing live logs as they become available.
             */
            liveUrl?: string;
        }

        export interface AppsGetTierResponse {
            tier?: outputs.apps.v2.AppsTier;
        }

        export interface AppsGitSourceSpec {
            /**
             * The name of the branch to use
             */
            branch?: string;
            /**
             * The clone URL of the repo. Example: `https://github.com/digitalocean/sample-golang.git`
             */
            repoCloneUrl?: string;
        }

        export interface AppsGithubSourceSpec {
            /**
             * The name of the branch to use
             */
            branch?: string;
            /**
             * Whether to automatically deploy new commits made to the repo
             */
            deployOnPush?: boolean;
            /**
             * The name of the repo in the format owner/repo. Example: `digitalocean/sample-golang`
             */
            repo?: string;
        }

        export interface AppsGitlabSourceSpec {
            /**
             * The name of the branch to use
             */
            branch?: string;
            /**
             * Whether to automatically deploy new commits made to the repo
             */
            deployOnPush?: boolean;
            /**
             * The name of the repo in the format owner/repo. Example: `digitalocean/sample-golang`
             */
            repo?: string;
        }

        export interface AppsImageSourceSpec {
            /**
             * The registry name. Must be left empty for the `DOCR` registry type.
             */
            registry?: string;
            /**
             * - DOCKER_HUB: The DockerHub container registry type.
             * - DOCR: The DigitalOcean container registry type.
             */
            registryType?: enums.apps.v2.AppsValidateAppSpecAppsImageSourceSpecRegistryType;
            /**
             * The repository name.
             */
            repository?: string;
            /**
             * The repository tag. Defaults to `latest` if not provided.
             */
            tag?: string;
        }
        /**
         * appsImageSourceSpecProvideDefaults sets the appropriate defaults for AppsImageSourceSpec
         */
        export function appsImageSourceSpecProvideDefaults(val: AppsImageSourceSpec): AppsImageSourceSpec {
            return {
                ...val,
                tag: (val.tag) ?? "latest",
            };
        }

        export interface AppsInstanceSize {
            cpuType?: enums.apps.v2.AppsInstanceSizeCpuType;
            cpus?: string;
            memoryBytes?: string;
            name?: string;
            slug?: string;
            tierDowngradeTo?: string;
            tierSlug?: string;
            tierUpgradeTo?: string;
            usdPerMonth?: string;
            usdPerSecond?: string;
        }
        /**
         * appsInstanceSizeProvideDefaults sets the appropriate defaults for AppsInstanceSize
         */
        export function appsInstanceSizeProvideDefaults(val: AppsInstanceSize): AppsInstanceSize {
            return {
                ...val,
                cpuType: (val.cpuType) ?? "UNSPECIFIED",
            };
        }

        export interface AppsListAlertsResponse {
            alerts?: outputs.apps.v2.AppAlert[];
        }

        export interface AppsListInstanceSizesResponse {
            discountPercent?: number;
            instanceSizes?: outputs.apps.v2.AppsInstanceSize[];
        }

        export interface AppsListRegionsResponse {
            regions?: outputs.apps.v2.AppsRegion[];
        }

        export interface AppsListTiersResponse {
            tiers?: outputs.apps.v2.AppsTier[];
        }

        export interface AppsRegion {
            continent?: string;
            dataCenters?: string[];
            /**
             * Whether or not the region is presented as the default.
             */
            default?: boolean;
            disabled?: boolean;
            flag?: string;
            label?: string;
            reason?: string;
            slug?: string;
        }

        export interface AppsResponse {
            apps?: outputs.apps.v2.App[];
            links?: outputs.apps.v2.PageLinks;
            meta: outputs.apps.v2.MetaMeta;
        }

        export interface AppsStringMatch {
            /**
             * Exact string match. Only 1 of `exact`, `prefix`, or `regex` must be set.
             */
            exact?: string;
            /**
             * Prefix-based match. Only 1 of `exact`, `prefix`, or `regex` must be set.
             */
            prefix?: string;
            /**
             * RE2 style regex-based match. Only 1 of `exact`, `prefix`, or `regex` must be set. For more information about RE2 syntax, see: https://github.com/google/re2/wiki/Syntax
             */
            regex?: string;
        }

        export interface AppsTier {
            buildSeconds?: string;
            egressBandwidthBytes?: string;
            name?: string;
            slug?: string;
            storageBytes?: string;
        }

        export interface Error {
            /**
             * A code identifier that represents the failing condition.
             *
             * Failing conditions:
             *   - `incompatible_phase` - indicates that the deployment's phase is not suitable for rollback.
             *   - `incompatible_result` - indicates that the deployment's result is not suitable for rollback.
             *   - `exceeded_revision_limit` - indicates that the app has exceeded the rollback revision limits for its tier.
             *   - `app_pinned` - indicates that there is already a rollback in progress and the app is pinned.
             *   - `database_config_conflict` - indicates that the deployment's database config is different than the current config.
             *   - `region_conflict` - indicates that the deployment's region differs from the current app region.
             *   
             * Warning conditions:
             *   - `static_site_requires_rebuild` - indicates that the deployment contains at least one static site that will require a rebuild.
             *   - `image_source_missing_digest` - indicates that the deployment contains at least one component with an image source that is missing a digest.
             */
            code?: enums.apps.v2.AppsValidateRollbackAppRollbackValidationConditionCode;
            components?: string[];
            /**
             * A human-readable message describing the failing condition.
             */
            message?: string;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.apps.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

    }
}

export namespace cdn {
    export namespace v2 {
        export interface CdnEndpoint {
            /**
             * The ID of a DigitalOcean managed TLS certificate used for SSL when a custom subdomain is provided.
             */
            certificateId?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the CDN endpoint was created.
             */
            createdAt?: string;
            /**
             * The fully qualified domain name (FQDN) of the custom subdomain used with the CDN endpoint.
             */
            customDomain?: string;
            /**
             * The fully qualified domain name (FQDN) from which the CDN-backed content is served.
             */
            endpoint?: string;
            /**
             * A unique ID that can be used to identify and reference a CDN endpoint.
             */
            id?: string;
            /**
             * The fully qualified domain name (FQDN) for the origin server which provides the content for the CDN. This is currently restricted to a Space.
             */
            origin: string;
            /**
             * The amount of time the content is cached by the CDN's edge servers in seconds. TTL must be one of 60, 600, 3600, 86400, or 604800. Defaults to 3600 (one hour) when excluded.
             */
            ttl?: enums.cdn.v2.CdnEndpointTtl;
        }
        /**
         * cdnEndpointProvideDefaults sets the appropriate defaults for CdnEndpoint
         */
        export function cdnEndpointProvideDefaults(val: CdnEndpoint): CdnEndpoint {
            return {
                ...val,
                ttl: (val.ttl) ?? 3600,
            };
        }

        export interface GetCdnEndpointProperties {
            endpoint?: outputs.cdn.v2.CdnEndpoint;
        }
        /**
         * getCdnEndpointPropertiesProvideDefaults sets the appropriate defaults for GetCdnEndpointProperties
         */
        export function getCdnEndpointPropertiesProvideDefaults(val: GetCdnEndpointProperties): GetCdnEndpointProperties {
            return {
                ...val,
                endpoint: (val.endpoint ? outputs.cdn.v2.cdnEndpointProvideDefaults(val.endpoint) : undefined),
            };
        }

        export interface ListCdnEndpointsItems {
            endpoints?: outputs.cdn.v2.CdnEndpoint[];
            links?: outputs.cdn.v2.PageLinks;
            meta: outputs.cdn.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.cdn.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

    }
}

export namespace certificates {
    export namespace v2 {
        export interface Certificate {
            /**
             * A unique identifier generated from the SHA-1 fingerprint of the certificate.
             */
            _sha1Fingerprint?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the certificate was created.
             */
            createdAt?: string;
            /**
             * An array of fully qualified domain names (FQDNs) for which the certificate was issued.
             */
            dnsNames?: string[];
            /**
             * A unique ID that can be used to identify and reference a certificate.
             */
            id?: string;
            /**
             * A unique human-readable name referring to a certificate.
             */
            name?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents the certificate's expiration date.
             */
            notAfter?: string;
            /**
             * A string representing the current state of the certificate. It may be `pending`, `verified`, or `error`.
             */
            state?: enums.certificates.v2.CertificateState;
            /**
             * A string representing the type of the certificate. The value will be `custom` for a user-uploaded certificate or `lets_encrypt` for one automatically generated with Let's Encrypt.
             */
            type?: enums.certificates.v2.CertificateType;
        }

        export interface GetCertificateProperties {
            certificate?: outputs.certificates.v2.Certificate;
        }

        export interface ListCertificatesItems {
            certificates?: outputs.certificates.v2.Certificate[];
            links?: outputs.certificates.v2.PageLinks;
            meta: outputs.certificates.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.certificates.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

    }
}

export namespace customers {
    export namespace v2 {
        export interface Balance {
            /**
             * Current balance of the customer's most recent billing activity.  Does not reflect `month_to_date_usage`.
             */
            accountBalance?: string;
            /**
             * The time at which balances were most recently generated.
             */
            generatedAt?: string;
            /**
             * Balance as of the `generated_at` time.  This value includes the `account_balance` and `month_to_date_usage`.
             */
            monthToDateBalance?: string;
            /**
             * Amount used in the current billing period as of the `generated_at` time.
             */
            monthToDateUsage?: string;
        }

        export interface BillingHistory {
            /**
             * Amount of the billing history entry.
             */
            amount?: string;
            /**
             * Time the billing history entry occurred.
             */
            date?: string;
            /**
             * Description of the billing history entry.
             */
            description?: string;
            /**
             * ID of the invoice associated with the billing history entry, if  applicable.
             */
            invoiceId?: string;
            /**
             * UUID of the invoice associated with the billing history entry, if  applicable.
             */
            invoiceUuid?: string;
            /**
             * Type of billing history entry.
             */
            type?: enums.customers.v2.BillingHistoryType;
        }

        /**
         * The invoice preview.
         */
        export interface InvoicePreview {
            /**
             * Total amount of the invoice, in USD.  This will reflect month-to-date usage in the invoice preview.
             */
            amount?: string;
            /**
             * Billing period of usage for which the invoice is issued, in `YYYY-MM`  format.
             */
            invoicePeriod?: string;
            /**
             * The UUID of the invoice. The canonical reference for the invoice.
             */
            invoiceUuid?: string;
            /**
             * Time the invoice was last updated.  This is only included with the invoice preview.
             */
            updatedAt?: string;
        }

        export interface ListBillingHistoryItems {
            billingHistory?: outputs.customers.v2.BillingHistory[];
            links?: outputs.customers.v2.PageLinks;
            /**
             * Information about the response itself.
             */
            meta: outputs.customers.v2.MetaProperties;
        }

        export interface ListInvoicesItems {
            /**
             * The invoice preview.
             */
            invoicePreview?: outputs.customers.v2.InvoicePreview;
            invoices?: outputs.customers.v2.InvoicePreview[];
            links?: outputs.customers.v2.PageLinks;
            meta: outputs.customers.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        /**
         * Information about the response itself.
         */
        export interface MetaProperties {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.customers.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

    }
}

export namespace databases {
    export namespace v2 {
        export interface Backup {
            /**
             * A time value given in ISO8601 combined date and time format at which the backup was created.
             */
            createdAt: string;
            /**
             * The size of the database backup in GBs.
             */
            sizeGigabytes: number;
        }

        export interface Ca {
            /**
             * base64 encoding of the certificate used to secure database connections
             */
            certificate: string;
        }

        export interface Connection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface ConnectionPool {
            connection?: outputs.databases.v2.ConnectionPoolConnection;
            /**
             * The database for use with the connection pool.
             */
            db: string;
            /**
             * The PGBouncer transaction mode for the connection pool. The allowed values are session, transaction, and statement.
             */
            mode: string;
            /**
             * A unique name for the connection pool. Must be between 3 and 60 characters.
             */
            name: string;
            privateConnection?: outputs.databases.v2.ConnectionPoolPrivateConnection;
            /**
             * The desired size of the PGBouncer connection pool. The maximum allowed size is determined by the size of the cluster's primary node. 25 backend server connections are allowed for every 1GB of RAM. Three are reserved for maintenance. For example, a primary node with 1 GB of RAM allows for a maximum of 22 backend server connections while one with 4 GB would allow for 97. Note that these are shared across all connection pools in a cluster.
             */
            size: number;
            /**
             * The name of the user for use with the connection pool. When excluded, all sessions connect to the database as the inbound user.
             */
            user?: string;
        }

        export interface ConnectionPoolConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface ConnectionPoolPrivateConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface ConnectionPools {
            /**
             * An array of connection pool objects.
             */
            pools?: outputs.databases.v2.ConnectionPool[];
        }

        export interface Creategres {
            /**
             * Specifies a fraction, in a decimal value, of the table size to add to autovacuum_analyze_threshold when deciding whether to trigger an ANALYZE. The default is 0.2 (20% of table size).
             */
            autovacuumAnalyzeScaleFactor?: number;
            /**
             * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an ANALYZE in any one table. The default is 50 tuples.
             */
            autovacuumAnalyzeThreshold?: number;
            /**
             * Specifies the maximum age (in transactions) that a table's pg_class.relfrozenxid field can attain before a VACUUM operation is forced to prevent transaction ID wraparound within the table. Note that the system will launch autovacuum processes to prevent wraparound even when autovacuum is otherwise disabled. This parameter will cause the server to be restarted.
             */
            autovacuumFreezeMaxAge?: number;
            /**
             * Specifies the maximum number of autovacuum processes (other than the autovacuum launcher) that may be running at any one time. The default is three. This parameter can only be set at server start.
             */
            autovacuumMaxWorkers?: number;
            /**
             * Specifies the minimum delay, in seconds, between autovacuum runs on any given database. The default is one minute.
             */
            autovacuumNaptime?: number;
            /**
             * Specifies the cost delay value, in milliseconds, that will be used in automatic VACUUM operations. If -1, uses the regular vacuum_cost_delay value, which is 20 milliseconds.
             */
            autovacuumVacuumCostDelay?: number;
            /**
             * Specifies the cost limit value that will be used in automatic VACUUM operations. If -1 is specified (which is the default), the regular vacuum_cost_limit value will be used.
             */
            autovacuumVacuumCostLimit?: number;
            /**
             * Specifies a fraction, in a decimal value, of the table size to add to autovacuum_vacuum_threshold when deciding whether to trigger a VACUUM. The default is 0.2 (20% of table size).
             */
            autovacuumVacuumScaleFactor?: number;
            /**
             * Specifies the minimum number of updated or deleted tuples needed to trigger a VACUUM in any one table. The default is 50 tuples.
             */
            autovacuumVacuumThreshold?: number;
            /**
             * The hour of day (in UTC) when backup for the service starts. New backup only starts if previous backup has already completed.
             */
            backupHour?: number;
            /**
             * The minute of the backup hour when backup for the service starts. New backup is only started if previous backup has already completed.
             */
            backupMinute?: number;
            /**
             * Specifies the delay, in milliseconds, between activity rounds for the background writer. Default is 200 ms.
             */
            bgwriterDelay?: number;
            /**
             * The amount of kilobytes that need to be written by the background writer before attempting to force the OS to issue these writes to underlying storage. Specified in kilobytes, default is 512.  Setting of 0 disables forced writeback.
             */
            bgwriterFlushAfter?: number;
            /**
             * The maximum number of buffers that the background writer can write. Setting this to zero disables background writing. Default is 100.
             */
            bgwriterLruMaxpages?: number;
            /**
             * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to bgwriter_lru_maxpages). 1.0 represents a “just in time” policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes. The default is 2.0.
             */
            bgwriterLruMultiplier?: number;
            /**
             * The amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
             */
            deadlockTimeout?: number;
            /**
             * Specifies the default TOAST compression method for values of compressible columns (the default is lz4).
             */
            defaultToastCompression?: enums.databases.v2.CreategresDefaultToastCompression;
            /**
             * Time out sessions with open transactions after this number of milliseconds
             */
            idleInTransactionSessionTimeout?: number;
            /**
             * Activates, in a boolean, the system-wide use of Just-in-Time Compilation (JIT).
             */
            jit?: boolean;
            /**
             * Causes each action executed by autovacuum to be logged if it ran for at least the specified number of milliseconds. Setting this to zero logs all autovacuum actions. Minus-one (the default) disables logging autovacuum actions.
             */
            logAutovacuumMinDuration?: number;
            /**
             * Controls the amount of detail written in the server log for each message that is logged.
             */
            logErrorVerbosity?: enums.databases.v2.CreategresLogErrorVerbosity;
            /**
             * Selects one of the available log-formats. These can support popular log analyzers like pgbadger, pganalyze, etc.
             */
            logLinePrefix?: enums.databases.v2.CreategresLogLinePrefix;
            /**
             * Log statements that take more than this number of milliseconds to run. If -1, disables.
             */
            logMinDurationStatement?: number;
            /**
             * PostgreSQL maximum number of files that can be open per process.
             */
            maxFilesPerProcess?: number;
            /**
             * PostgreSQL maximum locks per transaction. Once increased, this parameter cannot be lowered from its set value.
             */
            maxLocksPerTransaction?: number;
            /**
             * PostgreSQL maximum logical replication workers (taken from the pool of max_parallel_workers).
             */
            maxLogicalReplicationWorkers?: number;
            /**
             * Sets the maximum number of workers that the system can support for parallel queries.
             */
            maxParallelWorkers?: number;
            /**
             * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
             */
            maxParallelWorkersPerGather?: number;
            /**
             * PostgreSQL maximum predicate locks per transaction.
             */
            maxPredLocksPerTransaction?: number;
            /**
             * PostgreSQL maximum prepared transactions. Once increased, this parameter cannot be lowered from its set value.
             */
            maxPreparedTransactions?: number;
            /**
             * PostgreSQL maximum replication slots.
             */
            maxReplicationSlots?: number;
            /**
             * Maximum depth of the stack in bytes.
             */
            maxStackDepth?: number;
            /**
             * Max standby archive delay in milliseconds.
             */
            maxStandbyArchiveDelay?: number;
            /**
             * Max standby streaming delay in milliseconds.
             */
            maxStandbyStreamingDelay?: number;
            /**
             * PostgreSQL maximum WAL senders. Once increased, this parameter cannot be lowered from its set value.
             */
            maxWalSenders?: number;
            /**
             * Sets the maximum number of background processes that the system can support. Once increased, this parameter cannot be lowered from its set value.
             */
            maxWorkerProcesses?: number;
            /**
             * Sets the time interval to run pg_partman's scheduled tasks.
             */
            pgPartmanBgwInterval?: number;
            /**
             * Controls which role to use for pg_partman's scheduled background tasks. Must consist of alpha-numeric characters, dots, underscores, or dashes. May not start with dash or dot. Maximum of 64 characters.
             */
            pgPartmanBgwRole?: string;
            /**
             * Controls which statements are counted. Specify 'top' to track top-level statements (those issued directly by clients), 'all' to also track nested statements (such as statements invoked within functions), or 'none' to disable statement statistics collection. The default value is top.
             */
            pgStatStatementsTrack?: enums.databases.v2.CreategresPgStatStatementsTrack;
            /**
             * PGBouncer connection pooling settings
             */
            pgbouncer?: outputs.databases.v2.Pgbouncer;
            /**
             * Percentage of total RAM that the database server uses for shared memory buffers.  Valid range is 20-60 (float), which corresponds to 20% - 60%.  This setting adjusts the shared_buffers configuration value.
             */
            sharedBuffersPercentage?: number;
            /**
             * Enable the pg_stat_monitor extension. <b>Enabling this extension will cause the cluster to be restarted.</b> When this extension is enabled, pg_stat_statements results for utility commands are unreliable.
             */
            statMonitorEnable?: boolean;
            /**
             * Synchronous replication type. Note that the service plan also needs to support synchronous replication.
             */
            synchronousReplication?: enums.databases.v2.CreategresSynchronousReplication;
            /**
             * PostgreSQL temporary file limit in KiB. If -1, sets to unlimited.
             */
            tempFileLimit?: number;
            /**
             * TimescaleDB extension configuration values
             */
            timescaledb?: outputs.databases.v2.Timescaledb;
            /**
             * PostgreSQL service timezone
             */
            timezone?: string;
            /**
             * Specifies the number of bytes reserved to track the currently executing command for each active session.
             */
            trackActivityQuerySize?: number;
            /**
             * Record commit time of transactions.
             */
            trackCommitTimestamp?: enums.databases.v2.CreategresTrackCommitTimestamp;
            /**
             * Enables tracking of function call counts and time used.
             */
            trackFunctions?: enums.databases.v2.CreategresTrackFunctions;
            /**
             * Enables timing of database I/O calls. This parameter is off by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
             */
            trackIoTiming?: enums.databases.v2.CreategresTrackIoTiming;
            type?: string;
            /**
             * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to zero disables the timeout. Must be either 0 or between 5000 and 10800000.
             */
            walSenderTimeout?: number;
            /**
             * WAL flush interval in milliseconds. Note that setting this value to lower than the default 200ms may negatively impact performance
             */
            walWriterDelay?: number;
            /**
             * The maximum amount of memory, in MB, used by a query operation (such as a sort or hash table) before writing to temporary disk files. Default is 1MB + 0.075% of total RAM (up to 32MB).
             */
            workMem?: number;
        }
        /**
         * creategresProvideDefaults sets the appropriate defaults for Creategres
         */
        export function creategresProvideDefaults(val: Creategres): Creategres {
            return {
                ...val,
                type: (val.type) ?? "postgres",
            };
        }

        export interface Database {
            /**
             * The name of the database.
             */
            name: string;
        }

        export interface DatabaseBackup {
            /**
             * The timestamp of an existing database cluster backup in ISO8601 combined date and time format. The most recent backup will be used if excluded.
             */
            backupCreatedAt?: string;
            /**
             * The name of an existing database cluster from which the backup will be restored.
             */
            databaseName: string;
        }

        export interface DatabaseCluster {
            connection?: outputs.databases.v2.DatabaseClusterConnection;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the database cluster was created.
             */
            createdAt?: string;
            /**
             * An array of strings containing the names of databases created in the database cluster.
             */
            dbNames?: string[];
            /**
             * A slug representing the database engine used for the cluster. The possible values are: "pg" for PostgreSQL, "mysql" for MySQL, "redis" for Redis, and "mongodb" for MongoDB.
             */
            engine: enums.databases.v2.DatabaseClusterEngine;
            /**
             * A unique ID that can be used to identify and reference a database cluster.
             */
            id?: string;
            maintenanceWindow?: outputs.databases.v2.DatabaseClusterMaintenanceWindow;
            /**
             * A unique, human-readable name referring to a database cluster.
             */
            name: string;
            /**
             * The number of nodes in the database cluster.
             */
            numNodes: number;
            privateConnection?: outputs.databases.v2.DatabaseClusterPrivateConnection;
            /**
             * A string specifying the UUID of the VPC to which the database cluster will be assigned. If excluded, the cluster when creating a new database cluster, it will be assigned to your account's default VPC for the region.
             */
            privateNetworkUuid?: string;
            /**
             * The ID of the project that the database cluster is assigned to. If excluded when creating a new database cluster, it will be assigned to your default project.
             */
            projectId?: string;
            /**
             * The slug identifier for the region where the database cluster is located.
             */
            region: string;
            rules?: outputs.databases.v2.FirewallRule[];
            /**
             * A string representing the semantic version of the database engine in use for the cluster.
             */
            semanticVersion?: string;
            /**
             * The slug identifier representing the size of the nodes in the database cluster.
             */
            size: string;
            /**
             * A string representing the current status of the database cluster.
             */
            status?: enums.databases.v2.DatabaseClusterStatus;
            /**
             * An array of tags that have been applied to the database cluster.
             */
            tags?: string[];
            users?: outputs.databases.v2.DatabaseUser[];
            /**
             * A string representing the version of the database engine in use for the cluster.
             */
            version?: string;
            /**
             * A timestamp referring to the date when the particular version will no longer be available for creating new clusters. If null, the version does not have an end of availability timeline.
             */
            versionEndOfAvailability?: string;
            /**
             * A timestamp referring to the date when the particular version will no longer be supported. If null, the version does not have an end of life timeline.
             */
            versionEndOfLife?: string;
        }

        export interface DatabaseClusterConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface DatabaseClusterMaintenanceWindow {
            /**
             * The day of the week on which to apply maintenance updates.
             */
            day: string;
            /**
             * A list of strings, each containing information about a pending maintenance update.
             */
            description?: string[];
            /**
             * The hour in UTC at which maintenance updates will be applied in 24 hour format.
             */
            hour: string;
            /**
             * A boolean value indicating whether any maintenance is scheduled to be performed in the next window.
             */
            pending?: boolean;
        }

        export interface DatabaseClusterPrivateConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface DatabaseConfig {
            config?: outputs.databases.v2.Mysql | outputs.databases.v2.Creategres | outputs.databases.v2.Redis;
        }

        export interface DatabaseLayoutOption {
            numNodes?: number;
            /**
             * An array of objects containing the slugs available with various node counts
             */
            sizes?: string[];
        }

        export interface DatabaseReplica {
            connection?: outputs.databases.v2.DatabaseReplicaConnection;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the database cluster was created.
             */
            createdAt?: string;
            /**
             * A unique ID that can be used to identify and reference a database replica.
             */
            id?: string;
            /**
             * The name to give the read-only replicating
             */
            name: string;
            privateConnection?: outputs.databases.v2.DatabaseReplicaPrivateConnection;
            /**
             * A string specifying the UUID of the VPC to which the read-only replica will be assigned. If excluded, the replica will be assigned to your account's default VPC for the region.
             */
            privateNetworkUuid?: string;
            /**
             * A slug identifier for the region where the read-only replica will be located. If excluded, the replica will be placed in the same region as the cluster.
             */
            region?: string;
            /**
             * A slug identifier representing the size of the node for the read-only replica. The size of the replica must be at least as large as the node size for the database cluster from which it is replicating.
             */
            size?: string;
            /**
             * A string representing the current status of the database cluster.
             */
            status?: enums.databases.v2.DatabaseReplicaStatus;
            /**
             * A flat array of tag names as strings to apply to the read-only replica after it is created. Tag names can either be existing or new tags.
             */
            tags?: string[];
        }

        export interface DatabaseReplicaConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface DatabaseReplicaPrivateConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface DatabaseUser {
            mysqlSettings?: outputs.databases.v2.MysqlSettings;
            /**
             * The name of a database user.
             */
            name: string;
            /**
             * A randomly generated password for the database user.
             */
            password?: string;
            /**
             * A string representing the database user's role. The value will be either
             * "primary" or "normal".
             */
            role?: enums.databases.v2.DatabaseUserRole;
        }

        export interface DatabaseVersionAvailability {
            /**
             * A timestamp referring to the date when the particular version will no longer be available for creating new clusters. If null, the version does not have an end of availability timeline.
             */
            endOfAvailability?: string;
            /**
             * A timestamp referring to the date when the particular version will no longer be supported. If null, the version does not have an end of life timeline.
             */
            endOfLife?: string;
            /**
             * The engine version.
             */
            version?: string;
        }

        export interface FirewallRule {
            /**
             * A unique ID for the database cluster to which the rule is applied.
             */
            clusterUuid?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the firewall rule was created.
             */
            createdAt?: string;
            /**
             * The type of resource that the firewall rule allows to access the database cluster.
             */
            type: enums.databases.v2.FirewallRuleType;
            /**
             * A unique ID for the firewall rule itself.
             */
            uuid?: string;
            /**
             * The ID of the specific resource, the name of a tag applied to a group of resources, or the IP address that the firewall rule allows to access the database cluster.
             */
            value: string;
        }

        export interface GetDatabaseProperties {
            db: outputs.databases.v2.Database;
        }

        export interface GetDatabasesCaProperties {
            ca: outputs.databases.v2.Ca;
        }

        export interface GetDatabasesClusterProperties {
            database: outputs.databases.v2.DatabaseCluster;
        }

        export interface GetDatabasesConnectionPoolProperties {
            pool: outputs.databases.v2.ConnectionPool;
        }

        export interface GetDatabasesEvictionPolicyProperties {
            /**
             * A string specifying the desired eviction policy for the Redis cluster.
             *
             * - `noeviction`: Don't evict any data, returns error when memory limit is reached.
             * - `allkeys_lru:` Evict any key, least recently used (LRU) first.
             * - `allkeys_random`: Evict keys in a random order.
             * - `volatile_lru`: Evict keys with expiration only, least recently used (LRU) first.
             * - `volatile_random`: Evict keys with expiration only in a random order.
             * - `volatile_ttl`: Evict keys with expiration only, shortest time-to-live (TTL) first.
             */
            evictionPolicy: enums.databases.v2.GetDatabasesEvictionPolicyPropertiesEvictionPolicy;
        }

        export interface GetDatabasesReplicaProperties {
            replica?: outputs.databases.v2.DatabaseReplica;
        }

        export interface GetDatabasesUserProperties {
            user: outputs.databases.v2.DatabaseUser;
        }

        export interface ListDatabasesBackupsProperties {
            backups: outputs.databases.v2.Backup[];
        }

        export interface ListDatabasesClustersProperties {
            databases?: outputs.databases.v2.DatabaseCluster[];
        }

        export interface ListDatabasesFirewallRulesProperties {
            rules?: outputs.databases.v2.FirewallRule[];
        }

        export interface ListDatabasesProperties {
            dbs?: outputs.databases.v2.Database[];
        }

        export interface ListDatabasesReplicasProperties {
            replicas?: outputs.databases.v2.DatabaseReplica[];
        }

        export interface ListDatabasesUsersProperties {
            users?: outputs.databases.v2.DatabaseUser[];
        }

        export interface Mysql {
            /**
             * The hour of day (in UTC) when backup for the service starts. New backup only starts if previous backup has already completed.
             */
            backupHour?: number;
            /**
             * The minute of the backup hour when backup for the service starts. New backup  only starts if previous backup has already completed.
             */
            backupMinute?: number;
            /**
             * The minimum amount of time, in seconds, to keep binlog entries before deletion.  This may be extended for services that require binlog entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
             */
            binlogRetentionPeriod?: number;
            /**
             * The number of seconds that the mysqld server waits for a connect packet before responding with bad handshake.
             */
            connectTimeout?: number;
            /**
             * Default server time zone, in the form of an offset from UTC (from -12:00 to +12:00), a time zone name (EST), or 'SYSTEM' to use the MySQL server default.
             */
            defaultTimeZone?: string;
            /**
             * The maximum permitted result length, in bytes, for the GROUP_CONCAT() function.
             */
            groupConcatMaxLen?: number;
            /**
             * The time, in seconds, before cached statistics expire.
             */
            informationSchemaStatsExpiry?: number;
            /**
             * The minimum length of words that an InnoDB FULLTEXT index stores.
             */
            innodbFtMinTokenSize?: number;
            /**
             * The InnoDB FULLTEXT index stopword list for all InnoDB tables.
             */
            innodbFtServerStopwordTable?: string;
            /**
             * The time, in seconds, that an InnoDB transaction waits for a row lock. before giving up.
             */
            innodbLockWaitTimeout?: number;
            /**
             * The size of the buffer, in bytes, that InnoDB uses to write to the log files. on disk.
             */
            innodbLogBufferSize?: number;
            /**
             * The upper limit, in bytes, of the size of the temporary log files used during online DDL operations for InnoDB tables.
             */
            innodbOnlineAlterLogMaxSize?: number;
            /**
             * When enabled, records information about all deadlocks in InnoDB user transactions  in the error log. Disabled by default.
             */
            innodbPrintAllDeadlocks?: boolean;
            /**
             * When enabled, transaction timeouts cause InnoDB to abort and roll back the entire transaction.
             */
            innodbRollbackOnTimeout?: boolean;
            /**
             * The time, in seconds, the server waits for activity on an interactive. connection before closing it.
             */
            interactiveTimeout?: number;
            /**
             * The storage engine for in-memory internal temporary tables.
             */
            internalTmpMemStorageEngine?: enums.databases.v2.MysqlInternalTmpMemStorageEngine;
            /**
             * The time, in seconds, for a query to take to execute before  being captured by slow_query_logs. Default is 10 seconds.
             */
            longQueryTime?: number;
            /**
             * The size of the largest message, in bytes, that can be received by the server. Default is 67108864 (64M).
             */
            maxAllowedPacket?: number;
            /**
             * The maximum size, in bytes, of internal in-memory tables. Also set tmp_table_size. Default is 16777216 (16M)
             */
            maxHeapTableSize?: number;
            /**
             * The time, in seconds, to wait for more data from an existing connection. aborting the read.
             */
            netReadTimeout?: number;
            /**
             * The number of seconds to wait for a block to be written to a connection before aborting the write.
             */
            netWriteTimeout?: number;
            /**
             * When enabled, captures slow queries. When disabled, also truncates the mysql.slow_log table. Default is false.
             */
            slowQueryLog?: boolean;
            /**
             * The sort buffer size, in bytes, for ORDER BY optimization. Default is 262144. (256K).
             */
            sortBufferSize?: number;
            /**
             * Global SQL mode. If empty, uses MySQL server defaults. Must only include uppercase alphabetic characters, underscores, and commas.
             */
            sqlMode?: string;
            /**
             * Require primary key to be defined for new tables or old tables modified with ALTER TABLE and fail if missing. It is recommended to always have primary keys because various functionality may break if any large table is missing them.
             */
            sqlRequirePrimaryKey?: boolean;
            /**
             * The maximum size, in bytes, of internal in-memory tables. Also set max_heap_table_size. Default is 16777216 (16M).
             */
            tmpTableSize?: number;
            type?: string;
            /**
             * The number of seconds the server waits for activity on a noninteractive connection before closing it.
             */
            waitTimeout?: number;
        }
        /**
         * mysqlProvideDefaults sets the appropriate defaults for Mysql
         */
        export function mysqlProvideDefaults(val: Mysql): Mysql {
            return {
                ...val,
                type: (val.type) ?? "mysql",
            };
        }

        export interface MysqlSettings {
            /**
             * A string specifying the authentication method to be used for connections
             * to the MySQL user account. The valid values are `mysql_native_password`
             * or `caching_sha2_password`. If excluded when creating a new user, the
             * default for the version of MySQL in use will be used. As of MySQL 8.0, the
             * default is `caching_sha2_password`.
             */
            authPlugin: enums.databases.v2.MysqlSettingsAuthPlugin;
        }

        export interface OnlineMigration {
            /**
             * The time the migration was initiated, in ISO 8601 format.
             */
            createdAt?: string;
            /**
             * The ID of the most recent migration.
             */
            id?: string;
            /**
             * The current status of the migration.
             */
            status?: enums.databases.v2.OnlineMigrationStatus;
        }

        export interface Options {
            options?: outputs.databases.v2.OptionsOptionsProperties;
            versionAvailability?: outputs.databases.v2.OptionsVersionAvailabilityProperties;
        }

        export interface OptionsOptionsProperties {
            mongodb?: outputs.databases.v2.OptionsOptionsPropertiesMongodb;
            mysql?: outputs.databases.v2.OptionsOptionsPropertiesMysql;
            pg?: outputs.databases.v2.OptionsOptionsPropertiesPg;
            redis?: outputs.databases.v2.OptionsOptionsPropertiesRedis;
        }

        export interface OptionsOptionsPropertiesMongodb {
            /**
             * An array of objects, each indicating the node sizes (otherwise referred to as slugs) that are available with various numbers of nodes in the database cluster. Each slugs denotes the node's identifier, CPU, and RAM (in that order).
             */
            layouts?: outputs.databases.v2.DatabaseLayoutOption[];
            /**
             * An array of strings containing the names of available regions
             */
            regions?: string[];
            /**
             * An array of strings containing the names of available regions
             */
            versions?: string[];
        }

        export interface OptionsOptionsPropertiesMysql {
            /**
             * An array of objects, each indicating the node sizes (otherwise referred to as slugs) that are available with various numbers of nodes in the database cluster. Each slugs denotes the node's identifier, CPU, and RAM (in that order).
             */
            layouts?: outputs.databases.v2.DatabaseLayoutOption[];
            /**
             * An array of strings containing the names of available regions
             */
            regions?: string[];
            /**
             * An array of strings containing the names of available regions
             */
            versions?: string[];
        }

        export interface OptionsOptionsPropertiesPg {
            /**
             * An array of objects, each indicating the node sizes (otherwise referred to as slugs) that are available with various numbers of nodes in the database cluster. Each slugs denotes the node's identifier, CPU, and RAM (in that order).
             */
            layouts?: outputs.databases.v2.DatabaseLayoutOption[];
            /**
             * An array of strings containing the names of available regions
             */
            regions?: string[];
            /**
             * An array of strings containing the names of available regions
             */
            versions?: string[];
        }

        export interface OptionsOptionsPropertiesRedis {
            /**
             * An array of objects, each indicating the node sizes (otherwise referred to as slugs) that are available with various numbers of nodes in the database cluster. Each slugs denotes the node's identifier, CPU, and RAM (in that order).
             */
            layouts?: outputs.databases.v2.DatabaseLayoutOption[];
            /**
             * An array of strings containing the names of available regions
             */
            regions?: string[];
            /**
             * An array of strings containing the names of available regions
             */
            versions?: string[];
        }

        export interface OptionsVersionAvailabilityProperties {
            /**
             * An array of objects, each indicating the version end-of-life, end-of-availability for various database engines
             */
            mongodb?: outputs.databases.v2.DatabaseVersionAvailability[];
            /**
             * An array of objects, each indicating the version end-of-life, end-of-availability for various database engines
             */
            mysql?: outputs.databases.v2.DatabaseVersionAvailability[];
            /**
             * An array of objects, each indicating the version end-of-life, end-of-availability for various database engines
             */
            pg?: outputs.databases.v2.DatabaseVersionAvailability[];
            /**
             * An array of objects, each indicating the version end-of-life, end-of-availability for various database engines
             */
            redis?: outputs.databases.v2.DatabaseVersionAvailability[];
        }

        /**
         * PGBouncer connection pooling settings
         */
        export interface Pgbouncer {
            /**
             * If the automatically-created database pools have been unused this many seconds, they are freed. If 0, timeout is disabled.
             */
            autodbIdleTimeout?: number;
            /**
             * Only allows a maximum this many server connections per database (regardless of user). If 0, allows unlimited connections.
             */
            autodbMaxDbConnections?: number;
            /**
             * PGBouncer pool mode
             */
            autodbPoolMode?: enums.databases.v2.PgbouncerAutodbPoolMode;
            /**
             * If non-zero, automatically creates a pool of that size per user when a pool doesn't exist.
             */
            autodbPoolSize?: number;
            /**
             * List of parameters to ignore when given in startup packet.
             */
            ignoreStartupParameters?: enums.databases.v2.PgbouncerIgnoreStartupParametersItem[];
            /**
             * If current server connections are below this number, adds more. Improves behavior when usual load comes suddenly back after period of total inactivity. The value is effectively capped at the pool size.
             */
            minPoolSize?: number;
            /**
             * Drops server connections if they have been idle more than this many seconds.  If 0, timeout is disabled. 
             */
            serverIdleTimeout?: number;
            /**
             * The pooler closes any unused server connection that has been connected longer than this amount of seconds.
             */
            serverLifetime?: number;
            /**
             * Run server_reset_query (DISCARD ALL) in all pooling modes.
             */
            serverResetQueryAlways?: boolean;
        }

        export interface PrivateConnection {
            /**
             * The name of the default database.
             */
            database?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * A boolean value indicating if the connection should be made over SSL.
             */
            ssl?: boolean;
            /**
             * A connection string in the format accepted by the `psql` command. This is provided as a convenience and should be able to be constructed by the other attributes.
             */
            uri?: string;
            /**
             * The default user for the database.
             */
            user?: string;
        }

        export interface Redis {
            /**
             * Determines default pub/sub channels' ACL for new users if ACL is not supplied. When this option is not defined, all_channels is assumed to keep backward compatibility. This option doesn't affect Redis configuration acl-pubsub-default.
             */
            redisAclChannelsDefault?: enums.databases.v2.RedisRedisAclChannelsDefault;
            /**
             * Redis IO thread count
             */
            redisIoThreads?: number;
            /**
             * LFU maxmemory-policy counter decay time in minutes
             */
            redisLfuDecayTime?: number;
            /**
             * Counter logarithm factor for volatile-lfu and allkeys-lfu maxmemory-policies
             */
            redisLfuLogFactor?: number;
            /**
             * A string specifying the desired eviction policy for the Redis cluster.
             *
             * - `noeviction`: Don't evict any data, returns error when memory limit is reached.
             * - `allkeys_lru:` Evict any key, least recently used (LRU) first.
             * - `allkeys_random`: Evict keys in a random order.
             * - `volatile_lru`: Evict keys with expiration only, least recently used (LRU) first.
             * - `volatile_random`: Evict keys with expiration only in a random order.
             * - `volatile_ttl`: Evict keys with expiration only, shortest time-to-live (TTL) first.
             */
            redisMaxmemoryPolicy?: enums.databases.v2.RedisRedisMaxmemoryPolicy;
            /**
             * Set notify-keyspace-events option
             */
            redisNotifyKeyspaceEvents?: string;
            /**
             * Set number of redis databases. Changing this will cause a restart of redis service.
             */
            redisNumberOfDatabases?: number;
            /**
             * When persistence is 'rdb', Redis does RDB dumps each 10 minutes if any key is changed. Also RDB dumps are done according to backup schedule for backup purposes. When persistence is 'off', no RDB dumps and backups are done, so data can be lost at any moment if service is restarted for any reason, or if service is powered off. Also service can't be forked.
             */
            redisPersistence?: enums.databases.v2.RedisRedisPersistence;
            /**
             * Set output buffer limit for pub / sub clients in MB. The value is the hard limit, the soft limit is 1/4 of the hard limit. When setting the limit, be mindful of the available memory in the selected service plan.
             */
            redisPubsubClientOutputBufferLimit?: number;
            /**
             * Require SSL to access Redis
             */
            redisSsl?: boolean;
            /**
             * Redis idle connection timeout in seconds
             */
            redisTimeout?: number;
            type?: string;
        }
        /**
         * redisProvideDefaults sets the appropriate defaults for Redis
         */
        export function redisProvideDefaults(val: Redis): Redis {
            return {
                ...val,
                redisLfuDecayTime: (val.redisLfuDecayTime) ?? 1,
                redisLfuLogFactor: (val.redisLfuLogFactor) ?? 10,
                redisNotifyKeyspaceEvents: (val.redisNotifyKeyspaceEvents) ?? "",
                redisSsl: (val.redisSsl) ?? true,
                redisTimeout: (val.redisTimeout) ?? 300,
                type: (val.type) ?? "redis",
            };
        }

        export interface SourceProperties {
            /**
             * The name of the default database.
             */
            dbname?: string;
            /**
             * The FQDN pointing to the database cluster's current primary node.
             */
            host?: string;
            /**
             * The randomly generated password for the default user.
             */
            password?: string;
            /**
             * The port on which the database cluster is listening.
             */
            port?: number;
            /**
             * The default user for the database.
             */
            username?: string;
        }

        export interface SqlMode {
            /**
             * A string specifying the configured SQL modes for the MySQL cluster.
             */
            sqlMode: string;
        }

        /**
         * TimescaleDB extension configuration values
         */
        export interface Timescaledb {
            /**
             * The number of background workers for timescaledb operations.  Set to the sum of your number of databases and the total number of concurrent background workers you want running at any given point in time.
             */
            maxBackgroundWorkers?: number;
        }

    }
}

export namespace domains {
    export namespace v2 {
        export interface Domain {
            /**
             * This optional attribute may contain an IP address. When provided, an A record will be automatically created pointing to the apex domain.
             */
            ipAddress?: string;
            /**
             * The name of the domain itself. This should follow the standard domain format of domain.TLD. For instance, `example.com` is a valid domain name.
             */
            name?: string;
            /**
             * This value is the time to live for the records on this domain, in seconds. This defines the time frame that clients can cache queried information before a refresh should be requested.
             */
            ttl?: number;
            /**
             * This attribute contains the complete contents of the zone file for the selected domain. Individual domain record resources should be used to get more granular control over records. However, this attribute can also be used to get information about the SOA record, which is created automatically and is not accessible as an individual record resource.
             */
            zoneFile?: string;
        }

        export interface DomainRecord {
            /**
             * Variable data depending on record type. For example, the "data" value for an A record would be the IPv4 address to which the domain will be mapped. For a CAA record, it would contain the domain name of the CA being granted permission to issue certificates.
             */
            data?: string;
            /**
             * An unsigned integer between 0-255 used for CAA records.
             */
            flags?: number;
            /**
             * A unique identifier for each domain record.
             */
            id?: number;
            /**
             * The host name, alias, or service being defined by the record.
             */
            name?: string;
            /**
             * The port for SRV records.
             */
            port?: number;
            /**
             * The priority for SRV and MX records.
             */
            priority?: number;
            /**
             * The parameter tag for CAA records. Valid values are "issue", "issuewild", or "iodef"
             */
            tag?: string;
            /**
             * This value is the time to live for the record, in seconds. This defines the time frame that clients can cache queried information before a refresh should be requested.
             */
            ttl?: number;
            /**
             * The type of the DNS record. For example: A, CNAME, TXT, ...
             */
            type: string;
            /**
             * The weight for SRV records.
             */
            weight?: number;
        }

        export interface GetDomainProperties {
            domain?: outputs.domains.v2.Domain;
        }

        export interface GetDomainsRecordProperties {
            domainRecord?: outputs.domains.v2.DomainRecord;
        }

        export interface ListDomainsItems {
            /**
             * Array of volumes.
             */
            domains: outputs.domains.v2.Domain[];
            links?: outputs.domains.v2.PageLinks;
            meta: outputs.domains.v2.MetaMeta;
        }

        export interface ListDomainsRecordsItems {
            domainRecords?: outputs.domains.v2.DomainRecord[];
            links?: outputs.domains.v2.PageLinks;
            meta: outputs.domains.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.domains.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

    }
}

export namespace droplets {
    export namespace v2 {
        export interface Action {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            region?: outputs.droplets.v2.Region;
            regionSlug?: outputs.droplets.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.droplets.v2.DisableBackupsActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * actionProvideDefaults sets the appropriate defaults for Action
         */
        export function actionProvideDefaults(val: Action): Action {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        export interface ActionRegionSlug {
        }

        /**
         * An objects containing information about a resource associated with a Droplet.
         */
        export interface AssociatedResource {
            /**
             * The cost of the resource in USD per month if the resource is retained after the Droplet is destroyed.
             */
            cost?: string;
            /**
             * The unique identifier for the resource associated with the Droplet.
             */
            id?: string;
            /**
             * The name of the resource associated with the Droplet.
             */
            name?: string;
        }

        /**
         * An objects containing information about a resources scheduled for deletion.
         */
        export interface AssociatedResourceStatus {
            /**
             * A time value given in ISO8601 combined date and time format indicating when the requested action was completed.
             */
            completedAt?: string;
            /**
             * An object containing information about a resource scheduled for deletion.
             */
            droplet?: outputs.droplets.v2.DestroyedAssociatedResource;
            /**
             * A count of the associated resources that failed to be destroyed, if any.
             */
            failures?: number;
            /**
             * An object containing additional information about resource related to a Droplet requested to be destroyed.
             */
            resources?: outputs.droplets.v2.AssociatedResourceStatusResourcesProperties;
        }

        /**
         * An object containing additional information about resource related to a Droplet requested to be destroyed.
         */
        export interface AssociatedResourceStatusResourcesProperties {
            floatingIps?: outputs.droplets.v2.DestroyedAssociatedResource[];
            reservedIps?: outputs.droplets.v2.DestroyedAssociatedResource[];
            snapshots?: outputs.droplets.v2.DestroyedAssociatedResource[];
            volumeSnapshots?: outputs.droplets.v2.DestroyedAssociatedResource[];
            volumes?: outputs.droplets.v2.DestroyedAssociatedResource[];
        }

        /**
         * An object containing information about a resource scheduled for deletion.
         */
        export interface DestroyedAssociatedResource {
            /**
             * A time value given in ISO8601 combined date and time format indicating when the resource was destroyed if the request was successful.
             */
            destroyedAt?: string;
            /**
             * A string indicating that the resource was not successfully destroyed and providing additional information.
             */
            errorMessage?: string;
            /**
             * The unique identifier for the resource scheduled for deletion.
             */
            id?: string;
            /**
             * The name of the resource scheduled for deletion.
             */
            name?: string;
        }

        export interface Droplet {
            /**
             * An array of backup IDs of any backups that have been taken of the Droplet instance.  Droplet backups are enabled at the time of the instance creation.
             */
            backupIds: number[];
            /**
             * A time value given in ISO8601 combined date and time format that represents when the Droplet was created.
             */
            createdAt: string;
            /**
             * The size of the Droplet's disk in gigabytes.
             */
            disk: number;
            /**
             * An array of features enabled on this Droplet.
             */
            features: string[];
            /**
             * A unique identifier for each Droplet instance. This is automatically generated upon Droplet creation.
             */
            id: number;
            image: outputs.droplets.v2.Image;
            /**
             * **Note**: All Droplets created after March 2017 use internal kernels by default.
             * These Droplets will have this attribute set to `null`.
             *
             * The current [kernel](https://www.digitalocean.com/docs/droplets/how-to/kernel/)
             * for Droplets with externally managed kernels. This will initially be set to
             * the kernel of the base image when the Droplet is created.
             */
            kernel?: outputs.droplets.v2.Kernel;
            /**
             * A boolean value indicating whether the Droplet has been locked, preventing actions by users.
             */
            locked: boolean;
            /**
             * Memory of the Droplet in megabytes.
             */
            memory: number;
            /**
             * The human-readable name set for the Droplet instance.
             */
            name: string;
            /**
             * The details of the network that are configured for the Droplet instance.  This is an object that contains keys for IPv4 and IPv6.  The value of each of these is an array that contains objects describing an individual IP resource allocated to the Droplet.  These will define attributes like the IP address, netmask, and gateway of the specific network depending on the type of network it is.
             */
            networks: outputs.droplets.v2.DropletNetworksProperties;
            /**
             * The details of the Droplet's backups feature, if backups are configured for the Droplet. This object contains keys for the start and end times of the window during which the backup will start.
             */
            nextBackupWindow: outputs.droplets.v2.DropletNextBackupWindowProperties;
            region: outputs.droplets.v2.Region;
            size: outputs.droplets.v2.Size;
            /**
             * The unique slug identifier for the size of this Droplet.
             */
            sizeSlug: string;
            /**
             * An array of snapshot IDs of any snapshots created from the Droplet instance.
             */
            snapshotIds: number[];
            /**
             * A status string indicating the state of the Droplet instance. This may be "new", "active", "off", or "archive".
             */
            status: enums.droplets.v2.DropletStatus;
            /**
             * An array of Tags the Droplet has been tagged with.
             */
            tags: string[];
            /**
             * The number of virtual CPUs.
             */
            vcpus: number;
            /**
             * A flat array including the unique identifier for each Block Storage volume attached to the Droplet.
             */
            volumeIds: string[];
            /**
             * A string specifying the UUID of the VPC to which the Droplet is assigned.
             */
            vpcUuid?: string;
        }
        /**
         * dropletProvideDefaults sets the appropriate defaults for Droplet
         */
        export function dropletProvideDefaults(val: Droplet): Droplet {
            return {
                ...val,
                size: outputs.droplets.v2.sizeProvideDefaults(val.size),
            };
        }

        /**
         * The details of the network that are configured for the Droplet instance.  This is an object that contains keys for IPv4 and IPv6.  The value of each of these is an array that contains objects describing an individual IP resource allocated to the Droplet.  These will define attributes like the IP address, netmask, and gateway of the specific network depending on the type of network it is.
         */
        export interface DropletNetworksProperties {
            v4?: outputs.droplets.v2.NetworkV4[];
            v6?: outputs.droplets.v2.NetworkV6[];
        }

        /**
         * The details of the Droplet's backups feature, if backups are configured for the Droplet. This object contains keys for the start and end times of the window during which the backup will start.
         */
        export interface DropletNextBackupWindowProperties {
            /**
             * A time value given in ISO8601 combined date and time format specifying the end of the Droplet's backup window.
             */
            end?: string;
            /**
             * A time value given in ISO8601 combined date and time format specifying the start of the Droplet's backup window.
             */
            start?: string;
        }

        export interface DropletSnapshot {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the snapshot was created.
             */
            createdAt: string;
            /**
             * The minimum size in GB required for a volume or Droplet to use this snapshot.
             */
            minDiskSize: number;
            /**
             * A human-readable name for the snapshot.
             */
            name: string;
            /**
             * An array of the regions that the snapshot is available in. The regions are represented by their identifying slug values.
             */
            regions: string[];
            /**
             * The billable size of the snapshot in gigabytes.
             */
            sizeGigabytes: number;
            /**
             * Describes the kind of image. It may be one of `snapshot` or `backup`. This specifies whether an image is a user-generated Droplet snapshot or automatically created Droplet backup.
             */
            type: enums.droplets.v2.DropletSnapshotPropertiesType;
        }

        export interface Firewall {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the firewall was created.
             */
            createdAt?: string;
            /**
             * An array containing the IDs of the Droplets assigned to the firewall.
             */
            dropletIds?: number[];
            /**
             * A unique ID that can be used to identify and reference a firewall.
             */
            id?: string;
            inboundRules?: outputs.droplets.v2.FirewallRulesInboundRulesItem[];
            /**
             * A human-readable name for a firewall. The name must begin with an alphanumeric character. Subsequent characters must either be alphanumeric characters, a period (.), or a dash (-).
             */
            name?: string;
            outboundRules?: outputs.droplets.v2.FirewallRulesOutboundRulesItem[];
            /**
             * An array of objects each containing the fields "droplet_id", "removing", and "status". It is provided to detail exactly which Droplets are having their security policies updated. When empty, all changes have been successfully applied.
             */
            pendingChanges?: outputs.droplets.v2.FirewallPropertiesPendingChangesItemProperties[];
            /**
             * A status string indicating the current state of the firewall. This can be "waiting", "succeeded", or "failed".
             */
            status?: enums.droplets.v2.FirewallPropertiesStatus;
            tags?: outputs.droplets.v2.FirewallPropertiesTags;
        }

        export interface FirewallPropertiesPendingChangesItemProperties {
            dropletId?: number;
            removing?: boolean;
            status?: string;
        }

        export interface FirewallPropertiesTags {
        }

        export interface FirewallRulesInboundRulesItem {
            /**
             * The ports on which traffic will be allowed specified as a string containing a single port, a range (e.g. "8000-9000"), or "0" when all ports are open for a protocol. For ICMP rules this parameter will always return "0".
             */
            ports: string;
            /**
             * The type of traffic to be allowed. This may be one of `tcp`, `udp`, or `icmp`.
             */
            protocol: enums.droplets.v2.FirewallRuleBaseProtocol;
        }

        export interface FirewallRulesOutboundRulesItem {
            /**
             * The ports on which traffic will be allowed specified as a string containing a single port, a range (e.g. "8000-9000"), or "0" when all ports are open for a protocol. For ICMP rules this parameter will always return "0".
             */
            ports: string;
            /**
             * The type of traffic to be allowed. This may be one of `tcp`, `udp`, or `icmp`.
             */
            protocol: enums.droplets.v2.FirewallRuleBaseProtocol;
        }

        export interface GetDropletActionProperties {
            action?: outputs.droplets.v2.Action;
        }
        /**
         * getDropletActionPropertiesProvideDefaults sets the appropriate defaults for GetDropletActionProperties
         */
        export function getDropletActionPropertiesProvideDefaults(val: GetDropletActionProperties): GetDropletActionProperties {
            return {
                ...val,
                action: (val.action ? outputs.droplets.v2.actionProvideDefaults(val.action) : undefined),
            };
        }

        export interface GetDropletProperties {
            droplet?: outputs.droplets.v2.Droplet;
        }
        /**
         * getDropletPropertiesProvideDefaults sets the appropriate defaults for GetDropletProperties
         */
        export function getDropletPropertiesProvideDefaults(val: GetDropletProperties): GetDropletProperties {
            return {
                ...val,
                droplet: (val.droplet ? outputs.droplets.v2.dropletProvideDefaults(val.droplet) : undefined),
            };
        }

        export interface Image {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the image was created.
             */
            createdAt?: string;
            /**
             * An optional free-form text field to describe an image.
             */
            description?: string;
            /**
             * The name of a custom image's distribution. Currently, the valid values are  `Arch Linux`, `CentOS`, `CoreOS`, `Debian`, `Fedora`, `Fedora Atomic`,  `FreeBSD`, `Gentoo`, `openSUSE`, `RancherOS`, `Rocky Linux`, `Ubuntu`, and `Unknown`.  Any other value will be accepted but ignored, and `Unknown` will be used in its place.
             */
            distribution?: enums.droplets.v2.ImageDistribution;
            /**
             * A string containing information about errors that may occur when importing
             *  a custom image.
             */
            errorMessage?: string;
            /**
             * A unique number that can be used to identify and reference a specific image.
             */
            id?: number;
            /**
             * The minimum disk size in GB required for a Droplet to use this image.
             */
            minDiskSize?: number;
            /**
             * The display name that has been given to an image.  This is what is shown in the control panel and is generally a descriptive title for the image in question.
             */
            name?: string;
            /**
             * This is a boolean value that indicates whether the image in question is public or not. An image that is public is available to all accounts. A non-public image is only accessible from your account.
             */
            public?: boolean;
            /**
             * This attribute is an array of the regions that the image is available in. The regions are represented by their identifying slug values.
             */
            regions?: enums.droplets.v2.ImageRegionsItem[];
            /**
             * The size of the image in gigabytes.
             */
            sizeGigabytes?: number;
            /**
             * A uniquely identifying string that is associated with each of the DigitalOcean-provided public images. These can be used to reference a public image as an alternative to the numeric id.
             */
            slug?: string;
            /**
             * A status string indicating the state of a custom image. This may be `NEW`,
             *  `available`, `pending`, `deleted`, or `retired`.
             */
            status?: enums.droplets.v2.ImageStatus;
            /**
             * A flat array of tag names as strings to be applied to the resource. Tag names may be for either existing or new tags.
             */
            tags?: string[];
            /**
             * Describes the kind of image. It may be one of `base`, `snapshot`, `backup`, `custom`, or `admin`. Respectively, this specifies whether an image is a DigitalOcean base OS image, user-generated Droplet snapshot, automatically created Droplet backup, user-provided virtual machine image, or an image used for DigitalOcean managed resources (e.g. DOKS worker nodes).
             */
            type?: enums.droplets.v2.ImageType;
        }

        /**
         * **Note**: All Droplets created after March 2017 use internal kernels by default.
         * These Droplets will have this attribute set to `null`.
         *
         * The current [kernel](https://www.digitalocean.com/docs/droplets/how-to/kernel/)
         * for Droplets with externally managed kernels. This will initially be set to
         * the kernel of the base image when the Droplet is created.
         */
        export interface Kernel {
            /**
             * A unique number used to identify and reference a specific kernel.
             */
            id?: number;
            /**
             * The display name of the kernel. This is shown in the web UI and is generally a descriptive title for the kernel in question.
             */
            name?: string;
            /**
             * A standard kernel version string representing the version, patch, and release information.
             */
            version?: string;
        }

        export interface ListDropletActionsItems {
            actions?: outputs.droplets.v2.Action[];
            links?: outputs.droplets.v2.PageLinks;
            meta: outputs.droplets.v2.MetaMeta;
        }

        export interface ListDropletsAssociatedResourcesItems {
            floatingIps?: outputs.droplets.v2.AssociatedResource[];
            reservedIps?: outputs.droplets.v2.AssociatedResource[];
            snapshots?: outputs.droplets.v2.AssociatedResource[];
            volumeSnapshots?: outputs.droplets.v2.AssociatedResource[];
            volumes?: outputs.droplets.v2.AssociatedResource[];
        }

        export interface ListDropletsBackupsItems {
            backups?: outputs.droplets.v2.DropletSnapshot[];
            links?: outputs.droplets.v2.PageLinks;
            meta: outputs.droplets.v2.MetaMeta;
        }

        export interface ListDropletsFirewallsItems {
            firewalls?: outputs.droplets.v2.Firewall[];
            links?: outputs.droplets.v2.PageLinks;
            meta: outputs.droplets.v2.MetaMeta;
        }

        export interface ListDropletsItems {
            droplets?: outputs.droplets.v2.Droplet[];
            links?: outputs.droplets.v2.PageLinks;
            meta: outputs.droplets.v2.MetaMeta;
        }

        export interface ListDropletsKernelsItems {
            kernels?: outputs.droplets.v2.Kernel[];
            links?: outputs.droplets.v2.PageLinks;
            meta: outputs.droplets.v2.MetaMeta;
        }

        export interface ListDropletsNeighborsItems {
            droplets?: outputs.droplets.v2.Droplet[];
        }

        export interface ListDropletsSnapshotsItems {
            links?: outputs.droplets.v2.PageLinks;
            meta: outputs.droplets.v2.MetaMeta;
            snapshots?: outputs.droplets.v2.DropletSnapshot[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface NetworkV4 {
            /**
             * The gateway of the specified IPv4 network interface.
             *
             * For private interfaces, a gateway is not provided. This is denoted by
             * returning `nil` as its value.
             */
            gateway?: string;
            /**
             * The IP address of the IPv4 network interface.
             */
            ipAddress?: string;
            /**
             * The netmask of the IPv4 network interface.
             */
            netmask?: string;
            /**
             * The type of the IPv4 network interface.
             */
            type?: enums.droplets.v2.NetworkV4Type;
        }

        export interface NetworkV6 {
            /**
             * The gateway of the specified IPv6 network interface.
             */
            gateway?: string;
            /**
             * The IP address of the IPv6 network interface.
             */
            ipAddress?: string;
            /**
             * The netmask of the IPv6 network interface.
             */
            netmask?: number;
            /**
             * The type of the IPv6 network interface.
             *
             * **Note**: IPv6 private  networking is not currently supported.
             */
            type?: enums.droplets.v2.NetworkV6Type;
        }

        export interface PageLinks {
            pages?: outputs.droplets.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface Size {
            /**
             * This is a boolean value that represents whether new Droplets can be created with this size.
             */
            available: boolean;
            /**
             * A string describing the class of Droplets created from this size. For example: Basic, General Purpose, CPU-Optimized, Memory-Optimized, or Storage-Optimized.
             */
            description: string;
            /**
             * The amount of disk space set aside for Droplets of this size. The value is represented in gigabytes.
             */
            disk: number;
            /**
             * The amount of RAM allocated to Droplets created of this size. The value is represented in megabytes.
             */
            memory: number;
            /**
             * This describes the price of the Droplet size as measured hourly. The value is measured in US dollars.
             */
            priceHourly: number;
            /**
             * This attribute describes the monthly cost of this Droplet size if the Droplet is kept for an entire month. The value is measured in US dollars.
             */
            priceMonthly: number;
            /**
             * An array containing the region slugs where this size is available for Droplet creates.
             */
            regions: string[];
            /**
             * A human-readable string that is used to uniquely identify each size.
             */
            slug: string;
            /**
             * The amount of transfer bandwidth that is available for Droplets created in this size. This only counts traffic on the public interface. The value is given in terabytes.
             */
            transfer: number;
            /**
             * The integer of number CPUs allocated to Droplets of this size.
             */
            vcpus: number;
        }
        /**
         * sizeProvideDefaults sets the appropriate defaults for Size
         */
        export function sizeProvideDefaults(val: Size): Size {
            return {
                ...val,
                available: (val.available) ?? true,
            };
        }

    }
}

export namespace firewalls {
    export namespace v2 {
        export interface Firewall {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the firewall was created.
             */
            createdAt?: string;
            /**
             * An array containing the IDs of the Droplets assigned to the firewall.
             */
            dropletIds?: number[];
            /**
             * A unique ID that can be used to identify and reference a firewall.
             */
            id?: string;
            inboundRules?: outputs.firewalls.v2.FirewallRulesInboundRulesItem[];
            /**
             * A human-readable name for a firewall. The name must begin with an alphanumeric character. Subsequent characters must either be alphanumeric characters, a period (.), or a dash (-).
             */
            name?: string;
            outboundRules?: outputs.firewalls.v2.FirewallRulesOutboundRulesItem[];
            /**
             * An array of objects each containing the fields "droplet_id", "removing", and "status". It is provided to detail exactly which Droplets are having their security policies updated. When empty, all changes have been successfully applied.
             */
            pendingChanges?: outputs.firewalls.v2.FirewallPropertiesPendingChangesItemProperties[];
            /**
             * A status string indicating the current state of the firewall. This can be "waiting", "succeeded", or "failed".
             */
            status?: enums.firewalls.v2.FirewallPropertiesStatus;
            tags?: outputs.firewalls.v2.FirewallPropertiesTags;
        }

        export interface FirewallPropertiesPendingChangesItemProperties {
            dropletId?: number;
            removing?: boolean;
            status?: string;
        }

        export interface FirewallPropertiesTags {
        }

        export interface FirewallRulesInboundRulesItem {
            /**
             * The ports on which traffic will be allowed specified as a string containing a single port, a range (e.g. "8000-9000"), or "0" when all ports are open for a protocol. For ICMP rules this parameter will always return "0".
             */
            ports: string;
            /**
             * The type of traffic to be allowed. This may be one of `tcp`, `udp`, or `icmp`.
             */
            protocol: enums.firewalls.v2.FirewallsRulesFirewallRuleBaseProtocol;
        }

        export interface FirewallRulesOutboundRulesItem {
            /**
             * The ports on which traffic will be allowed specified as a string containing a single port, a range (e.g. "8000-9000"), or "0" when all ports are open for a protocol. For ICMP rules this parameter will always return "0".
             */
            ports: string;
            /**
             * The type of traffic to be allowed. This may be one of `tcp`, `udp`, or `icmp`.
             */
            protocol: enums.firewalls.v2.FirewallsRulesFirewallRuleBaseProtocol;
        }

        export interface GetFirewallProperties {
            firewall?: outputs.firewalls.v2.Firewall;
        }

        export interface ListFirewallsItems {
            firewalls?: outputs.firewalls.v2.Firewall[];
            links?: outputs.firewalls.v2.PageLinks;
            meta: outputs.firewalls.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.firewalls.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Tags {
        }

    }
}

export namespace floating_ips {
    export namespace v2 {
        export interface Action {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            /**
             * The UUID of the project to which the reserved IP currently belongs.
             */
            projectId?: string;
            region?: outputs.floating_ips.v2.Region;
            regionSlug?: outputs.floating_ips.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.floating_ips.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * actionProvideDefaults sets the appropriate defaults for Action
         */
        export function actionProvideDefaults(val: Action): Action {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        /**
         * The linked actions can be used to check the status of a Droplet's create event.
         */
        export interface ActionLink {
            /**
             * A URL that can be used to access the action.
             */
            href?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            /**
             * A string specifying the type of the related action.
             */
            rel?: string;
        }

        export interface ActionRegionSlug {
        }

        export interface Droplet {
            /**
             * An array of backup IDs of any backups that have been taken of the Droplet instance.  Droplet backups are enabled at the time of the instance creation.
             */
            backupIds: number[];
            /**
             * A time value given in ISO8601 combined date and time format that represents when the Droplet was created.
             */
            createdAt: string;
            /**
             * The size of the Droplet's disk in gigabytes.
             */
            disk: number;
            /**
             * An array of features enabled on this Droplet.
             */
            features: string[];
            /**
             * A unique identifier for each Droplet instance. This is automatically generated upon Droplet creation.
             */
            id: number;
            image: outputs.floating_ips.v2.Image;
            /**
             * **Note**: All Droplets created after March 2017 use internal kernels by default.
             * These Droplets will have this attribute set to `null`.
             *
             * The current [kernel](https://www.digitalocean.com/docs/droplets/how-to/kernel/)
             * for Droplets with externally managed kernels. This will initially be set to
             * the kernel of the base image when the Droplet is created.
             */
            kernel?: outputs.floating_ips.v2.Kernel;
            /**
             * A boolean value indicating whether the Droplet has been locked, preventing actions by users.
             */
            locked: boolean;
            /**
             * Memory of the Droplet in megabytes.
             */
            memory: number;
            /**
             * The human-readable name set for the Droplet instance.
             */
            name: string;
            /**
             * The details of the network that are configured for the Droplet instance.  This is an object that contains keys for IPv4 and IPv6.  The value of each of these is an array that contains objects describing an individual IP resource allocated to the Droplet.  These will define attributes like the IP address, netmask, and gateway of the specific network depending on the type of network it is.
             */
            networks: outputs.floating_ips.v2.DropletNetworksProperties;
            /**
             * The details of the Droplet's backups feature, if backups are configured for the Droplet. This object contains keys for the start and end times of the window during which the backup will start.
             */
            nextBackupWindow: outputs.floating_ips.v2.DropletNextBackupWindowProperties;
            region: outputs.floating_ips.v2.Region;
            size: outputs.floating_ips.v2.Size;
            /**
             * The unique slug identifier for the size of this Droplet.
             */
            sizeSlug: string;
            /**
             * An array of snapshot IDs of any snapshots created from the Droplet instance.
             */
            snapshotIds: number[];
            /**
             * A status string indicating the state of the Droplet instance. This may be "new", "active", "off", or "archive".
             */
            status: enums.floating_ips.v2.DropletStatus;
            /**
             * An array of Tags the Droplet has been tagged with.
             */
            tags: string[];
            /**
             * The number of virtual CPUs.
             */
            vcpus: number;
            /**
             * A flat array including the unique identifier for each Block Storage volume attached to the Droplet.
             */
            volumeIds: string[];
            /**
             * A string specifying the UUID of the VPC to which the Droplet is assigned.
             */
            vpcUuid?: string;
        }
        /**
         * dropletProvideDefaults sets the appropriate defaults for Droplet
         */
        export function dropletProvideDefaults(val: Droplet): Droplet {
            return {
                ...val,
                size: outputs.floating_ips.v2.sizeProvideDefaults(val.size),
            };
        }

        /**
         * The details of the network that are configured for the Droplet instance.  This is an object that contains keys for IPv4 and IPv6.  The value of each of these is an array that contains objects describing an individual IP resource allocated to the Droplet.  These will define attributes like the IP address, netmask, and gateway of the specific network depending on the type of network it is.
         */
        export interface DropletNetworksProperties {
            v4?: outputs.floating_ips.v2.NetworkV4[];
            v6?: outputs.floating_ips.v2.NetworkV6[];
        }

        /**
         * The details of the Droplet's backups feature, if backups are configured for the Droplet. This object contains keys for the start and end times of the window during which the backup will start.
         */
        export interface DropletNextBackupWindowProperties {
            /**
             * A time value given in ISO8601 combined date and time format specifying the end of the Droplet's backup window.
             */
            end?: string;
            /**
             * A time value given in ISO8601 combined date and time format specifying the start of the Droplet's backup window.
             */
            start?: string;
        }

        export interface FloatingIp {
            /**
             * The Droplet that the floating IP has been assigned to. When you query a floating IP, if it is assigned to a Droplet, the entire Droplet object will be returned. If it is not assigned, the value will be null.
             */
            droplet?: outputs.floating_ips.v2.Droplet;
            /**
             * The public IP address of the floating IP. It also serves as its identifier.
             */
            ip?: string;
            /**
             * A boolean value indicating whether or not the floating IP has pending actions preventing new ones from being submitted.
             */
            locked?: boolean;
            /**
             * The UUID of the project to which the reserved IP currently belongs.
             */
            projectId?: string;
            region?: outputs.floating_ips.v2.FloatingIpRegion;
        }
        /**
         * floatingIpProvideDefaults sets the appropriate defaults for FloatingIp
         */
        export function floatingIpProvideDefaults(val: FloatingIp): FloatingIp {
            return {
                ...val,
                droplet: (val.droplet ? outputs.floating_ips.v2.dropletProvideDefaults(val.droplet) : undefined),
            };
        }

        export interface FloatingIpRegion {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface GetFloatingIPProperties {
            floatingIp?: outputs.floating_ips.v2.FloatingIp;
        }
        /**
         * getFloatingIPPropertiesProvideDefaults sets the appropriate defaults for GetFloatingIPProperties
         */
        export function getFloatingIPPropertiesProvideDefaults(val: GetFloatingIPProperties): GetFloatingIPProperties {
            return {
                ...val,
                floatingIp: (val.floatingIp ? outputs.floating_ips.v2.floatingIpProvideDefaults(val.floatingIp) : undefined),
            };
        }

        export interface GetFloatingIPsActionProperties {
            action?: outputs.floating_ips.v2.GetFloatingIPsActionPropertiesAction;
        }
        /**
         * getFloatingIPsActionPropertiesProvideDefaults sets the appropriate defaults for GetFloatingIPsActionProperties
         */
        export function getFloatingIPsActionPropertiesProvideDefaults(val: GetFloatingIPsActionProperties): GetFloatingIPsActionProperties {
            return {
                ...val,
                action: (val.action ? outputs.floating_ips.v2.getFloatingIPsActionPropertiesActionProvideDefaults(val.action) : undefined),
            };
        }

        export interface GetFloatingIPsActionPropertiesAction {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            /**
             * The UUID of the project to which the reserved IP currently belongs.
             */
            projectId?: string;
            region?: outputs.floating_ips.v2.Region;
            regionSlug?: outputs.floating_ips.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.floating_ips.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * getFloatingIPsActionPropertiesActionProvideDefaults sets the appropriate defaults for GetFloatingIPsActionPropertiesAction
         */
        export function getFloatingIPsActionPropertiesActionProvideDefaults(val: GetFloatingIPsActionPropertiesAction): GetFloatingIPsActionPropertiesAction {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        export interface Image {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the image was created.
             */
            createdAt?: string;
            /**
             * An optional free-form text field to describe an image.
             */
            description?: string;
            /**
             * The name of a custom image's distribution. Currently, the valid values are  `Arch Linux`, `CentOS`, `CoreOS`, `Debian`, `Fedora`, `Fedora Atomic`,  `FreeBSD`, `Gentoo`, `openSUSE`, `RancherOS`, `Rocky Linux`, `Ubuntu`, and `Unknown`.  Any other value will be accepted but ignored, and `Unknown` will be used in its place.
             */
            distribution?: enums.floating_ips.v2.ImageDistribution;
            /**
             * A string containing information about errors that may occur when importing
             *  a custom image.
             */
            errorMessage?: string;
            /**
             * A unique number that can be used to identify and reference a specific image.
             */
            id?: number;
            /**
             * The minimum disk size in GB required for a Droplet to use this image.
             */
            minDiskSize?: number;
            /**
             * The display name that has been given to an image.  This is what is shown in the control panel and is generally a descriptive title for the image in question.
             */
            name?: string;
            /**
             * This is a boolean value that indicates whether the image in question is public or not. An image that is public is available to all accounts. A non-public image is only accessible from your account.
             */
            public?: boolean;
            /**
             * This attribute is an array of the regions that the image is available in. The regions are represented by their identifying slug values.
             */
            regions?: enums.floating_ips.v2.ImageRegionsItem[];
            /**
             * The size of the image in gigabytes.
             */
            sizeGigabytes?: number;
            /**
             * A uniquely identifying string that is associated with each of the DigitalOcean-provided public images. These can be used to reference a public image as an alternative to the numeric id.
             */
            slug?: string;
            /**
             * A status string indicating the state of a custom image. This may be `NEW`,
             *  `available`, `pending`, `deleted`, or `retired`.
             */
            status?: enums.floating_ips.v2.ImageStatus;
            /**
             * A flat array of tag names as strings to be applied to the resource. Tag names may be for either existing or new tags.
             */
            tags?: string[];
            /**
             * Describes the kind of image. It may be one of `base`, `snapshot`, `backup`, `custom`, or `admin`. Respectively, this specifies whether an image is a DigitalOcean base OS image, user-generated Droplet snapshot, automatically created Droplet backup, user-provided virtual machine image, or an image used for DigitalOcean managed resources (e.g. DOKS worker nodes).
             */
            type?: enums.floating_ips.v2.ImageType;
        }

        /**
         * **Note**: All Droplets created after March 2017 use internal kernels by default.
         * These Droplets will have this attribute set to `null`.
         *
         * The current [kernel](https://www.digitalocean.com/docs/droplets/how-to/kernel/)
         * for Droplets with externally managed kernels. This will initially be set to
         * the kernel of the base image when the Droplet is created.
         */
        export interface Kernel {
            /**
             * A unique number used to identify and reference a specific kernel.
             */
            id?: number;
            /**
             * The display name of the kernel. This is shown in the web UI and is generally a descriptive title for the kernel in question.
             */
            name?: string;
            /**
             * A standard kernel version string representing the version, patch, and release information.
             */
            version?: string;
        }

        export interface LinksProperties {
            actions?: outputs.floating_ips.v2.ActionLink[];
            droplets?: outputs.floating_ips.v2.ActionLink[];
        }

        export interface ListFloatingIPsActionItems {
            actions?: outputs.floating_ips.v2.Action[];
            links?: outputs.floating_ips.v2.PageLinks;
            meta: outputs.floating_ips.v2.MetaMeta;
        }

        export interface ListFloatingIPsItems {
            floatingIps?: outputs.floating_ips.v2.FloatingIp[];
            links?: outputs.floating_ips.v2.PageLinks;
            meta: outputs.floating_ips.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface NetworkV4 {
            /**
             * The gateway of the specified IPv4 network interface.
             *
             * For private interfaces, a gateway is not provided. This is denoted by
             * returning `nil` as its value.
             */
            gateway?: string;
            /**
             * The IP address of the IPv4 network interface.
             */
            ipAddress?: string;
            /**
             * The netmask of the IPv4 network interface.
             */
            netmask?: string;
            /**
             * The type of the IPv4 network interface.
             */
            type?: enums.floating_ips.v2.NetworkV4Type;
        }

        export interface NetworkV6 {
            /**
             * The gateway of the specified IPv6 network interface.
             */
            gateway?: string;
            /**
             * The IP address of the IPv6 network interface.
             */
            ipAddress?: string;
            /**
             * The netmask of the IPv6 network interface.
             */
            netmask?: number;
            /**
             * The type of the IPv6 network interface.
             *
             * **Note**: IPv6 private  networking is not currently supported.
             */
            type?: enums.floating_ips.v2.NetworkV6Type;
        }

        export interface PageLinks {
            pages?: outputs.floating_ips.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface Size {
            /**
             * This is a boolean value that represents whether new Droplets can be created with this size.
             */
            available: boolean;
            /**
             * A string describing the class of Droplets created from this size. For example: Basic, General Purpose, CPU-Optimized, Memory-Optimized, or Storage-Optimized.
             */
            description: string;
            /**
             * The amount of disk space set aside for Droplets of this size. The value is represented in gigabytes.
             */
            disk: number;
            /**
             * The amount of RAM allocated to Droplets created of this size. The value is represented in megabytes.
             */
            memory: number;
            /**
             * This describes the price of the Droplet size as measured hourly. The value is measured in US dollars.
             */
            priceHourly: number;
            /**
             * This attribute describes the monthly cost of this Droplet size if the Droplet is kept for an entire month. The value is measured in US dollars.
             */
            priceMonthly: number;
            /**
             * An array containing the region slugs where this size is available for Droplet creates.
             */
            regions: string[];
            /**
             * A human-readable string that is used to uniquely identify each size.
             */
            slug: string;
            /**
             * The amount of transfer bandwidth that is available for Droplets created in this size. This only counts traffic on the public interface. The value is given in terabytes.
             */
            transfer: number;
            /**
             * The integer of number CPUs allocated to Droplets of this size.
             */
            vcpus: number;
        }
        /**
         * sizeProvideDefaults sets the appropriate defaults for Size
         */
        export function sizeProvideDefaults(val: Size): Size {
            return {
                ...val,
                available: (val.available) ?? true,
            };
        }

    }
}

export namespace functions {
    export namespace v2 {
        export interface GetFunctionsNamespaceProperties {
            namespace?: outputs.functions.v2.NamespaceInfo;
        }

        export interface GetFunctionsTriggerProperties {
            trigger?: outputs.functions.v2.TriggerInfo;
        }

        export interface ListFunctionsNamespacesItems {
            namespaces?: outputs.functions.v2.NamespaceInfo[];
        }

        export interface ListFunctionsTriggersItems {
            triggers?: outputs.functions.v2.TriggerInfo[];
        }

        export interface NamespaceInfo {
            /**
             * The namespace's API hostname. Each function in a namespace is provided an endpoint at the namespace's hostname.
             */
            apiHost?: string;
            /**
             * UTC time string.
             */
            createdAt?: string;
            /**
             * A random alpha numeric string. This key is used in conjunction with the namespace's UUID to authenticate 
             * a user to use the namespace via `doctl`, DigitalOcean's official CLI.
             */
            key?: string;
            /**
             * The namespace's unique name.
             */
            label?: string;
            /**
             * A unique string format of UUID with a prefix fn-.
             */
            namespace?: string;
            /**
             * The namespace's datacenter region.
             */
            region?: string;
            /**
             * UTC time string.
             */
            updatedAt?: string;
            /**
             * The namespace's Universally Unique Identifier.
             */
            uuid?: string;
        }

        /**
         * Trigger details for SCHEDULED type, where body is optional.
         */
        export interface ScheduledDetails {
            /**
             * Optional data to be sent to function while triggering the function.
             */
            body?: outputs.functions.v2.ScheduledDetailsBodyProperties;
            /**
             * valid cron expression string which is required for SCHEDULED type triggers.
             */
            cron: string;
        }

        /**
         * Optional data to be sent to function while triggering the function.
         */
        export interface ScheduledDetailsBodyProperties {
            name?: string;
        }

        export interface TriggerInfo {
            /**
             * UTC time string.
             */
            createdAt?: string;
            /**
             * Name of function(action) that exists in the given namespace.
             */
            function?: string;
            /**
             * Indicates weather the trigger is paused or unpaused.
             */
            isEnabled?: boolean;
            /**
             * The trigger's unique name within the namespace.
             */
            name?: string;
            /**
             * A unique string format of UUID with a prefix fn-.
             */
            namespace?: string;
            /**
             * Trigger details for SCHEDULED type, where body is optional.
             */
            scheduledDetails?: outputs.functions.v2.ScheduledDetails;
            scheduledRuns?: outputs.functions.v2.TriggerInfoScheduledRunsProperties;
            /**
             * String which indicates the type of trigger source like SCHEDULED.
             */
            type?: string;
            /**
             * UTC time string.
             */
            updatedAt?: string;
        }

        export interface TriggerInfoScheduledRunsProperties {
            /**
             * Indicates last run time. null value indicates trigger not run yet.
             */
            lastRunAt?: string;
            /**
             * Indicates next run time. null value indicates trigger will not run.
             */
            nextRunAt?: string;
        }

    }
}

export namespace images {
    export namespace v2 {
        export interface Action {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            region?: outputs.images.v2.Region;
            regionSlug?: outputs.images.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.images.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * actionProvideDefaults sets the appropriate defaults for Action
         */
        export function actionProvideDefaults(val: Action): Action {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        export interface ActionRegionSlug {
        }

        export interface GetImageProperties {
            image: outputs.images.v2.Image;
        }

        export interface Image {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the image was created.
             */
            createdAt?: string;
            /**
             * An optional free-form text field to describe an image.
             */
            description?: string;
            /**
             * The name of a custom image's distribution. Currently, the valid values are  `Arch Linux`, `CentOS`, `CoreOS`, `Debian`, `Fedora`, `Fedora Atomic`,  `FreeBSD`, `Gentoo`, `openSUSE`, `RancherOS`, `Rocky Linux`, `Ubuntu`, and `Unknown`.  Any other value will be accepted but ignored, and `Unknown` will be used in its place.
             */
            distribution?: enums.images.v2.ImageDistribution;
            /**
             * A string containing information about errors that may occur when importing
             *  a custom image.
             */
            errorMessage?: string;
            /**
             * A unique number that can be used to identify and reference a specific image.
             */
            id?: number;
            /**
             * The minimum disk size in GB required for a Droplet to use this image.
             */
            minDiskSize?: number;
            /**
             * The display name that has been given to an image.  This is what is shown in the control panel and is generally a descriptive title for the image in question.
             */
            name?: string;
            /**
             * This is a boolean value that indicates whether the image in question is public or not. An image that is public is available to all accounts. A non-public image is only accessible from your account.
             */
            public?: boolean;
            /**
             * This attribute is an array of the regions that the image is available in. The regions are represented by their identifying slug values.
             */
            regions?: enums.images.v2.ImageRegionsItem[];
            /**
             * The size of the image in gigabytes.
             */
            sizeGigabytes?: number;
            /**
             * A uniquely identifying string that is associated with each of the DigitalOcean-provided public images. These can be used to reference a public image as an alternative to the numeric id.
             */
            slug?: string;
            /**
             * A status string indicating the state of a custom image. This may be `NEW`,
             *  `available`, `pending`, `deleted`, or `retired`.
             */
            status?: enums.images.v2.ImageStatus;
            /**
             * A flat array of tag names as strings to be applied to the resource. Tag names may be for either existing or new tags.
             */
            tags?: string[];
            /**
             * Describes the kind of image. It may be one of `base`, `snapshot`, `backup`, `custom`, or `admin`. Respectively, this specifies whether an image is a DigitalOcean base OS image, user-generated Droplet snapshot, automatically created Droplet backup, user-provided virtual machine image, or an image used for DigitalOcean managed resources (e.g. DOKS worker nodes).
             */
            type?: enums.images.v2.ImageType;
        }

        export interface ListImageActionsItems {
            actions?: outputs.images.v2.Action[];
            links?: outputs.images.v2.PageLinks;
            meta: outputs.images.v2.MetaMeta;
        }

        export interface ListImagesItems {
            images: outputs.images.v2.Image[];
            links?: outputs.images.v2.PageLinks;
            meta: outputs.images.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.images.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface RegionSlug {
        }

    }
}

export namespace kubernetes {
    export namespace v2 {
        export interface AssociatedKubernetesResource {
            /**
             * The ID of a resource associated with a Kubernetes cluster.
             */
            id?: string;
            /**
             * The name of a resource associated with a Kubernetes cluster.
             */
            name?: string;
        }

        /**
         * An object containing the IDs of resources associated with a Kubernetes cluster.
         */
        export interface AssociatedKubernetesResources {
            /**
             * A list of names and IDs for associated load balancers that can be destroyed along with the cluster.
             */
            loadBalancers?: outputs.kubernetes.v2.AssociatedKubernetesResource[];
            /**
             * A list of names and IDs for associated volume snapshots that can be destroyed along with the cluster.
             */
            volumeSnapshots?: outputs.kubernetes.v2.AssociatedKubernetesResource[];
            /**
             * A list of names and IDs for associated volumes that can be destroyed along with the cluster.
             */
            volumes?: outputs.kubernetes.v2.AssociatedKubernetesResource[];
        }

        export interface Cluster {
            /**
             * A boolean value indicating whether the cluster will be automatically upgraded to new patch releases during its maintenance window.
             */
            autoUpgrade?: boolean;
            /**
             * The range of IP addresses in the overlay network of the Kubernetes cluster in CIDR notation.
             */
            clusterSubnet?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the Kubernetes cluster was created.
             */
            createdAt?: string;
            /**
             * The base URL of the API server on the Kubernetes master node.
             */
            endpoint?: string;
            /**
             * A boolean value indicating whether the control plane is run in a highly available configuration in the cluster. Highly available control planes incur less downtime. The property cannot be disabled.
             */
            ha?: boolean;
            /**
             * A unique ID that can be used to identify and reference a Kubernetes cluster.
             */
            id?: string;
            /**
             * The public IPv4 address of the Kubernetes master node. This will not be set if high availability is configured on the cluster (v1.21+)
             */
            ipv4?: string;
            /**
             * An object specifying the maintenance window policy for the Kubernetes cluster.
             */
            maintenancePolicy?: outputs.kubernetes.v2.MaintenancePolicy;
            /**
             * A human-readable name for a Kubernetes cluster.
             */
            name: string;
            /**
             * An object specifying the details of the worker nodes available to the Kubernetes cluster.
             */
            nodePools: outputs.kubernetes.v2.KubernetesNodePool[];
            /**
             * The slug identifier for the region where the Kubernetes cluster is located.
             */
            region: string;
            /**
             * A read-only boolean value indicating if a container registry is integrated with the cluster.
             */
            registryEnabled?: boolean;
            /**
             * The range of assignable IP addresses for services running in the Kubernetes cluster in CIDR notation.
             */
            serviceSubnet?: string;
            /**
             * An object containing a `state` attribute whose value is set to a string indicating the current status of the cluster.
             */
            status?: outputs.kubernetes.v2.ClusterStatusProperties;
            /**
             * A boolean value indicating whether surge upgrade is enabled/disabled for the cluster. Surge upgrade makes cluster upgrades fast and reliable by bringing up new nodes before destroying the outdated nodes.
             */
            surgeUpgrade?: boolean;
            /**
             * An array of tags applied to the Kubernetes cluster. All clusters are automatically tagged `k8s` and `k8s:$K8S_CLUSTER_ID`.
             */
            tags?: string[];
            /**
             * A time value given in ISO8601 combined date and time format that represents when the Kubernetes cluster was last updated.
             */
            updatedAt?: string;
            /**
             * The slug identifier for the version of Kubernetes used for the cluster. If set to a minor version (e.g. "1.14"), the latest version within it will be used (e.g. "1.14.6-do.1"); if set to "latest", the latest published version will be used. See the `/v2/kubernetes/options` endpoint to find all currently available versions.
             */
            version: string;
            /**
             * A string specifying the UUID of the VPC to which the Kubernetes cluster is assigned.
             */
            vpcUuid?: string;
        }
        /**
         * clusterProvideDefaults sets the appropriate defaults for Cluster
         */
        export function clusterProvideDefaults(val: Cluster): Cluster {
            return {
                ...val,
                autoUpgrade: (val.autoUpgrade) ?? false,
                ha: (val.ha) ?? false,
                surgeUpgrade: (val.surgeUpgrade) ?? false,
            };
        }

        /**
         * An object containing a `state` attribute whose value is set to a string indicating the current status of the cluster.
         */
        export interface ClusterStatusProperties {
            /**
             * An optional message providing additional information about the current cluster state.
             */
            message?: string;
            /**
             * A string indicating the current status of the cluster.
             */
            state?: enums.kubernetes.v2.ClusterStatusPropertiesState;
        }

        export interface ClusterlintResults {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the schedule clusterlint run request was completed.
             */
            completedAt?: string;
            /**
             * An array of diagnostics reporting potential problems for the given cluster.
             */
            diagnostics?: outputs.kubernetes.v2.ClusterlintResultsDiagnosticsItemProperties[];
            /**
             * A time value given in ISO8601 combined date and time format that represents when the schedule clusterlint run request was made.
             */
            requestedAt?: string;
            /**
             * Id of the clusterlint run that can be used later to fetch the diagnostics.
             */
            runId?: string;
        }

        export interface ClusterlintResultsDiagnosticsItemProperties {
            /**
             * The clusterlint check that resulted in the diagnostic.
             */
            checkName?: string;
            /**
             * Feedback about the object for users to fix.
             */
            message?: string;
            /**
             * Metadata about the Kubernetes API object the diagnostic is reported on.
             */
            object?: outputs.kubernetes.v2.ClusterlintResultsDiagnosticsItemPropertiesObjectProperties;
            /**
             * Can be one of error, warning or suggestion.
             */
            severity?: string;
        }

        /**
         * Metadata about the Kubernetes API object the diagnostic is reported on.
         */
        export interface ClusterlintResultsDiagnosticsItemPropertiesObjectProperties {
            /**
             * The kind of Kubernetes API object
             */
            kind?: string;
            /**
             * Name of the object
             */
            name?: string;
            /**
             * The namespace the object resides in the cluster.
             */
            namespace?: string;
        }

        export interface Credentials {
            /**
             * A base64 encoding of bytes representing the certificate authority data for accessing the cluster.
             */
            certificateAuthorityData?: string;
            /**
             * A base64 encoding of bytes representing the x509 client
             * certificate data for access the cluster. This is only returned for clusters
             * without support for token-based authentication.
             *
             * Newly created Kubernetes clusters do not return credentials using
             * certificate-based authentication. For additional information,
             * [see here](https://www.digitalocean.com/docs/kubernetes/how-to/connect-to-cluster/#authenticate).
             */
            clientCertificateData?: string;
            /**
             * A base64 encoding of bytes representing the x509 client key
             * data for access the cluster. This is only returned for clusters without
             * support for token-based authentication.
             *
             * Newly created Kubernetes clusters do not return credentials using
             * certificate-based authentication. For additional information,
             * [see here](https://www.digitalocean.com/docs/kubernetes/how-to/connect-to-cluster/#authenticate).
             */
            clientKeyData?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the access token expires.
             */
            expiresAt?: string;
            /**
             * The URL used to access the cluster API server.
             */
            server?: string;
            /**
             * An access token used to authenticate with the cluster. This is only returned for clusters with support for token-based authentication.
             */
            token?: string;
        }

        export interface GetKubernetesAvailableUpgradeProperties {
            availableUpgradeVersions?: outputs.kubernetes.v2.KubernetesVersion[];
        }

        export interface GetKubernetesClusterProperties {
            kubernetesCluster?: outputs.kubernetes.v2.Cluster;
        }
        /**
         * getKubernetesClusterPropertiesProvideDefaults sets the appropriate defaults for GetKubernetesClusterProperties
         */
        export function getKubernetesClusterPropertiesProvideDefaults(val: GetKubernetesClusterProperties): GetKubernetesClusterProperties {
            return {
                ...val,
                kubernetesCluster: (val.kubernetesCluster ? outputs.kubernetes.v2.clusterProvideDefaults(val.kubernetesCluster) : undefined),
            };
        }

        export interface GetKubernetesNodePoolProperties {
            nodePool?: outputs.kubernetes.v2.KubernetesNodePool;
        }

        export interface KubernetesNodePool {
            /**
             * A boolean value indicating whether auto-scaling is enabled for this node pool.
             */
            autoScale?: boolean;
            /**
             * The number of Droplet instances in the node pool.
             */
            count?: number;
            /**
             * A unique ID that can be used to identify and reference a specific node pool.
             */
            id?: string;
            /**
             * An object of key/value mappings specifying labels to apply to all nodes in a pool. Labels will automatically be applied to all existing nodes and any subsequent nodes added to the pool. Note that when a label is removed, it is not deleted from the nodes in the pool.
             */
            labels?: any;
            /**
             * The maximum number of nodes that this node pool can be auto-scaled to. The value will be `0` if `auto_scale` is set to `false`.
             */
            maxNodes?: number;
            /**
             * The minimum number of nodes that this node pool can be auto-scaled to. The value will be `0` if `auto_scale` is set to `false`.
             */
            minNodes?: number;
            /**
             * A human-readable name for the node pool.
             */
            name?: string;
            /**
             * An object specifying the details of a specific worker node in a node pool.
             */
            nodes?: outputs.kubernetes.v2.Node[];
            /**
             * The slug identifier for the type of Droplet used as workers in the node pool.
             */
            size?: string;
            /**
             * An array containing the tags applied to the node pool. All node pools are automatically tagged `k8s`, `k8s-worker`, and `k8s:$K8S_CLUSTER_ID`.
             */
            tags?: string[];
            /**
             * An array of taints to apply to all nodes in a pool. Taints will automatically be applied to all existing nodes and any subsequent nodes added to the pool. When a taint is removed, it is deleted from all nodes in the pool.
             */
            taints?: outputs.kubernetes.v2.KubernetesNodePoolTaint[];
        }

        export interface KubernetesNodePoolTaint {
            /**
             * How the node reacts to pods that it won't tolerate. Available effect values are `NoSchedule`, `PreferNoSchedule`, and `NoExecute`.
             */
            effect?: enums.kubernetes.v2.KubernetesNodePoolTaintEffect;
            /**
             * An arbitrary string. The `key` and `value` fields of the `taint` object form a key-value pair. For example, if the value of the `key` field is "special" and the value of the `value` field is "gpu", the key value pair would be `special=gpu`.
             */
            key?: string;
            /**
             * An arbitrary string. The `key` and `value` fields of the `taint` object form a key-value pair. For example, if the value of the `key` field is "special" and the value of the `value` field is "gpu", the key value pair would be `special=gpu`.
             */
            value?: string;
        }

        export interface KubernetesOptions {
            options?: outputs.kubernetes.v2.KubernetesOptionsOptionsProperties;
        }

        export interface KubernetesOptionsOptionsProperties {
            regions?: outputs.kubernetes.v2.KubernetesRegion[];
            sizes?: outputs.kubernetes.v2.KubernetesSize[];
            versions?: outputs.kubernetes.v2.KubernetesVersion[];
        }

        export interface KubernetesRegion {
            /**
             * A DigitalOcean region where Kubernetes is available.
             */
            name?: string;
            /**
             * The identifier for a region for use when creating a new cluster.
             */
            slug?: string;
        }

        export interface KubernetesSize {
            /**
             * A Droplet size available for use in a Kubernetes node pool.
             */
            name?: string;
            /**
             * The identifier for a size for use when creating a new cluster.
             */
            slug?: string;
        }

        export interface KubernetesVersion {
            /**
             * The upstream version string for the version of Kubernetes provided by a given slug.
             */
            kubernetesVersion?: string;
            /**
             * The slug identifier for an available version of Kubernetes for use when creating or updating a cluster. The string contains both the upstream version of Kubernetes as well as the DigitalOcean revision.
             */
            slug?: string;
            /**
             * The features available with the version of Kubernetes provided by a given slug.
             */
            supportedFeatures?: string[];
        }

        export interface ListKubernetesClustersItems {
            kubernetesClusters?: outputs.kubernetes.v2.Cluster[];
            links?: outputs.kubernetes.v2.PageLinks;
            meta: outputs.kubernetes.v2.MetaMeta;
        }

        export interface ListKubernetesNodePoolsProperties {
            nodePools?: outputs.kubernetes.v2.KubernetesNodePool[];
        }

        /**
         * An object specifying the maintenance window policy for the Kubernetes cluster.
         */
        export interface MaintenancePolicy {
            /**
             * The day of the maintenance window policy. May be one of `monday` through `sunday`, or `any` to indicate an arbitrary week day.
             */
            day?: enums.kubernetes.v2.MaintenancePolicyDay;
            /**
             * The duration of the maintenance window policy in human-readable format.
             */
            duration?: string;
            /**
             * The start time in UTC of the maintenance window policy in 24-hour clock format / HH:MM notation (e.g., `15:00`).
             */
            startTime?: string;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface Node {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the node was created.
             */
            createdAt?: string;
            /**
             * The ID of the Droplet used for the worker node.
             */
            dropletId?: string;
            /**
             * A unique ID that can be used to identify and reference the node.
             */
            id?: string;
            /**
             * An automatically generated, human-readable name for the node.
             */
            name?: string;
            /**
             * An object containing a `state` attribute whose value is set to a string indicating the current status of the node.
             */
            status?: outputs.kubernetes.v2.NodeStatusProperties;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the node was last updated.
             */
            updatedAt?: string;
        }

        /**
         * An object containing a `state` attribute whose value is set to a string indicating the current status of the node.
         */
        export interface NodeStatusProperties {
            /**
             * A string indicating the current status of the node.
             */
            state?: enums.kubernetes.v2.NodeStatusPropertiesState;
        }

        export interface PageLinks {
            pages?: outputs.kubernetes.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        /**
         * An object containing a `state` attribute whose value is set to a string indicating the current status of the cluster.
         */
        export interface StatusProperties {
            /**
             * An optional message providing additional information about the current cluster state.
             */
            message?: string;
            /**
             * A string indicating the current status of the cluster.
             */
            state?: enums.kubernetes.v2.KubernetesClusterStatusPropertiesState;
        }

        export interface User {
            kubernetesClusterUser?: outputs.kubernetes.v2.UserKubernetesClusterUserProperties;
        }

        export interface UserKubernetesClusterUserProperties {
            /**
             * A list of in-cluster groups that the user belongs to.
             */
            groups?: string[];
            /**
             * The username for the cluster admin user.
             */
            username?: string;
        }

    }
}

export namespace load_balancers {
    export namespace v2 {
        /**
         * An object specifying a forwarding rule for a load balancer.
         */
        export interface ForwardingRule {
            /**
             * The ID of the TLS certificate used for SSL termination if enabled.
             */
            certificateId?: string;
            /**
             * An integer representing the port on which the load balancer instance will listen.
             */
            entryPort: number;
            /**
             * The protocol used for traffic to the load balancer. The possible values are: `http`, `https`, `http2`, `http3`, `tcp`, or `udp`. If you set the  `entry_protocol` to `udp`, the `target_protocol` must be set to `udp`.  When using UDP, the load balancer requires that you set up a health  check with a port that uses TCP, HTTP, or HTTPS to work properly.
             */
            entryProtocol: enums.load_balancers.v2.ForwardingRuleEntryProtocol;
            /**
             * An integer representing the port on the backend Droplets to which the load balancer will send traffic.
             */
            targetPort: number;
            /**
             * The protocol used for traffic from the load balancer to the backend Droplets. The possible values are: `http`, `https`, `http2`, `tcp`, or `udp`. If you set the `target_protocol` to `udp`, the `entry_protocol` must be set to  `udp`. When using UDP, the load balancer requires that you set up a health  check with a port that uses TCP, HTTP, or HTTPS to work properly.
             */
            targetProtocol: enums.load_balancers.v2.ForwardingRuleTargetProtocol;
            /**
             * A boolean value indicating whether SSL encrypted traffic will be passed through to the backend Droplets.
             */
            tlsPassthrough?: boolean;
        }

        export interface GetLoadBalancerProperties {
            loadBalancer?: outputs.load_balancers.v2.LoadBalancer;
        }
        /**
         * getLoadBalancerPropertiesProvideDefaults sets the appropriate defaults for GetLoadBalancerProperties
         */
        export function getLoadBalancerPropertiesProvideDefaults(val: GetLoadBalancerProperties): GetLoadBalancerProperties {
            return {
                ...val,
                loadBalancer: (val.loadBalancer ? outputs.load_balancers.v2.loadBalancerProvideDefaults(val.loadBalancer) : undefined),
            };
        }

        /**
         * An object specifying health check settings for the load balancer.
         */
        export interface HealthCheck {
            /**
             * The number of seconds between between two consecutive health checks.
             */
            checkIntervalSeconds?: number;
            /**
             * The number of times a health check must pass for a backend Droplet to be marked "healthy" and be re-added to the pool.
             */
            healthyThreshold?: number;
            /**
             * The path on the backend Droplets to which the load balancer instance will send a request.
             */
            path?: string;
            /**
             * An integer representing the port on the backend Droplets on which the health check will attempt a connection.
             */
            port?: number;
            /**
             * The protocol used for health checks sent to the backend Droplets. The possible values are `http`, `https`, or `tcp`.
             */
            protocol?: enums.load_balancers.v2.HealthCheckProtocol;
            /**
             * The number of seconds the load balancer instance will wait for a response until marking a health check as failed.
             */
            responseTimeoutSeconds?: number;
            /**
             * The number of times a health check must fail for a backend Droplet to be marked "unhealthy" and be removed from the pool.
             */
            unhealthyThreshold?: number;
        }
        /**
         * healthCheckProvideDefaults sets the appropriate defaults for HealthCheck
         */
        export function healthCheckProvideDefaults(val: HealthCheck): HealthCheck {
            return {
                ...val,
                checkIntervalSeconds: (val.checkIntervalSeconds) ?? 10,
                healthyThreshold: (val.healthyThreshold) ?? 3,
                path: (val.path) ?? "/",
                port: (val.port) ?? 80,
                protocol: (val.protocol) ?? "http",
                responseTimeoutSeconds: (val.responseTimeoutSeconds) ?? 5,
                unhealthyThreshold: (val.unhealthyThreshold) ?? 5,
            };
        }

        /**
         * An object specifying allow and deny rules to control traffic to the load balancer.
         */
        export interface LbFirewall {
            /**
             * the rules for allowing traffic to the load balancer (in the form 'ip:1.2.3.4' or 'cidr:1.2.0.0/16')
             */
            allow?: string[];
            /**
             * the rules for denying traffic to the load balancer (in the form 'ip:1.2.3.4' or 'cidr:1.2.0.0/16')
             */
            deny?: string[];
        }

        export interface ListLoadBalancersItems {
            links?: outputs.load_balancers.v2.PageLinks;
            loadBalancers?: outputs.load_balancers.v2.LoadBalancer[];
            meta: outputs.load_balancers.v2.MetaMeta;
        }

        export interface LoadBalancer {
            /**
             * This field has been deprecated. You can no longer specify an algorithm for load balancers.
             */
            algorithm?: enums.load_balancers.v2.LoadBalancerBaseAlgorithm;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the load balancer was created.
             */
            createdAt?: string;
            /**
             * A boolean value indicating whether to disable automatic DNS record creation for Let's Encrypt certificates that are added to the load balancer.
             */
            disableLetsEncryptDnsRecords?: boolean;
            /**
             * A boolean value indicating whether HTTP keepalive connections are maintained to target Droplets.
             */
            enableBackendKeepalive?: boolean;
            /**
             * A boolean value indicating whether PROXY Protocol is in use.
             */
            enableProxyProtocol?: boolean;
            /**
             * An object specifying allow and deny rules to control traffic to the load balancer.
             */
            firewall?: outputs.load_balancers.v2.LbFirewall;
            /**
             * An array of objects specifying the forwarding rules for a load balancer.
             */
            forwardingRules: outputs.load_balancers.v2.ForwardingRule[];
            /**
             * An object specifying health check settings for the load balancer.
             */
            healthCheck?: outputs.load_balancers.v2.HealthCheck;
            /**
             * An integer value which configures the idle timeout for HTTP requests to the target droplets.
             */
            httpIdleTimeoutSeconds?: number;
            /**
             * A unique ID that can be used to identify and reference a load balancer.
             */
            id?: string;
            /**
             * An attribute containing the public-facing IP address of the load balancer.
             */
            ip?: string;
            /**
             * A human-readable name for a load balancer instance.
             */
            name?: string;
            /**
             * The ID of the project that the load balancer is associated with. If no ID is provided at creation, the load balancer associates with the user's default project. If an invalid project ID is provided, the load balancer will not be created.
             */
            projectId?: string;
            /**
             * A boolean value indicating whether HTTP requests to the load balancer on port 80 will be redirected to HTTPS on port 443.
             */
            redirectHttpToHttps?: boolean;
            /**
             * This field has been replaced by the `size_unit` field for all regions except in AMS2, NYC2, and SFO1. Each available load balancer size now equates to the load balancer having a set number of nodes.
             * * `lb-small` = 1 node
             * * `lb-medium` = 3 nodes
             * * `lb-large` = 6 nodes
             *
             * You can resize load balancers after creation up to once per hour. You cannot resize a load balancer within the first hour of its creation.
             */
            size?: enums.load_balancers.v2.LoadBalancerBaseSize;
            /**
             * How many nodes the load balancer contains. Each additional node increases the load balancer's ability to manage more connections. Load balancers can be scaled up or down, and you can change the number of nodes after creation up to once per hour. This field is currently not available in the AMS2, NYC2, or SFO1 regions. Use the `size` field to scale load balancers that reside in these regions.
             */
            sizeUnit?: number;
            /**
             * A status string indicating the current state of the load balancer. This can be `new`, `active`, or `errored`.
             */
            status?: enums.load_balancers.v2.LoadBalancerBaseStatus;
            /**
             * An object specifying sticky sessions settings for the load balancer.
             */
            stickySessions?: outputs.load_balancers.v2.StickySessions;
            /**
             * The name of a Droplet tag corresponding to Droplets assigned to the load balancer.
             */
            tag?: string;
            /**
             * A string specifying the UUID of the VPC to which the load balancer is assigned.
             */
            vpcUuid?: string;
        }
        /**
         * loadBalancerProvideDefaults sets the appropriate defaults for LoadBalancer
         */
        export function loadBalancerProvideDefaults(val: LoadBalancer): LoadBalancer {
            return {
                ...val,
                algorithm: (val.algorithm) ?? "round_robin",
                disableLetsEncryptDnsRecords: (val.disableLetsEncryptDnsRecords) ?? false,
                enableBackendKeepalive: (val.enableBackendKeepalive) ?? false,
                enableProxyProtocol: (val.enableProxyProtocol) ?? false,
                healthCheck: (val.healthCheck ? outputs.load_balancers.v2.healthCheckProvideDefaults(val.healthCheck) : undefined),
                httpIdleTimeoutSeconds: (val.httpIdleTimeoutSeconds) ?? 60,
                redirectHttpToHttps: (val.redirectHttpToHttps) ?? false,
                size: (val.size) ?? "lb-small",
                sizeUnit: (val.sizeUnit) ?? 1,
                stickySessions: (val.stickySessions ? outputs.load_balancers.v2.stickySessionsProvideDefaults(val.stickySessions) : undefined),
            };
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.load_balancers.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        /**
         * An object specifying sticky sessions settings for the load balancer.
         */
        export interface StickySessions {
            /**
             * The name of the cookie sent to the client. This attribute is only returned when using `cookies` for the sticky sessions type.
             */
            cookieName?: string;
            /**
             * The number of seconds until the cookie set by the load balancer expires. This attribute is only returned when using `cookies` for the sticky sessions type.
             */
            cookieTtlSeconds?: number;
            /**
             * An attribute indicating how and if requests from a client will be persistently served by the same backend Droplet. The possible values are `cookies` or `none`.
             */
            type?: enums.load_balancers.v2.StickySessionsType;
        }
        /**
         * stickySessionsProvideDefaults sets the appropriate defaults for StickySessions
         */
        export function stickySessionsProvideDefaults(val: StickySessions): StickySessions {
            return {
                ...val,
                type: (val.type) ?? "none",
            };
        }

    }
}

export namespace monitoring {
    export namespace v2 {
        export interface AlertPolicy {
            alerts: outputs.monitoring.v2.Alerts;
            compare: enums.monitoring.v2.AlertPolicyCompare;
            description: string;
            enabled: boolean;
            entities: string[];
            tags: string[];
            type: enums.monitoring.v2.AlertPolicyType;
            uuid: string;
            value: number;
            window: enums.monitoring.v2.AlertPolicyWindow;
        }

        export interface Alerts {
            /**
             * An email to notify on an alert trigger.
             */
            email: string[];
            /**
             * Slack integration details.
             */
            slack: outputs.monitoring.v2.SlackDetails[];
        }

        export interface GetMonitoringAlertPolicyProperties {
            policy?: outputs.monitoring.v2.AlertPolicy;
        }

        export interface ListMonitoringAlertPolicyItems {
            links?: outputs.monitoring.v2.PageLinks;
            meta: outputs.monitoring.v2.MetaMeta;
            policies: outputs.monitoring.v2.AlertPolicy[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface Metrics {
            data: outputs.monitoring.v2.MetricsData;
            status: enums.monitoring.v2.MetricsStatus;
        }

        export interface MetricsData {
            /**
             * Result of query.
             */
            result: outputs.monitoring.v2.MetricsResult[];
            resultType: enums.monitoring.v2.MetricsDataResultType;
        }

        export interface MetricsResult {
            /**
             * An object containing the metric labels.
             */
            metric: any;
            values: (number | string)[][];
        }

        export interface PageLinks {
            pages?: outputs.monitoring.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface SlackDetails {
            /**
             * Slack channel to notify of an alert trigger.
             */
            channel: string;
            /**
             * Slack Webhook URL.
             */
            url: string;
        }

    }
}

export namespace oneclicks {
    export namespace v2 {
        export interface ListOneClicksProperties {
            _1Clicks?: outputs.oneclicks.v2.OneClicks[];
        }

        export interface OneClicks {
            /**
             * The slug identifier for the 1-Click application.
             */
            slug: string;
            /**
             * The type of the 1-Click application.
             */
            type: string;
        }

    }
}

export namespace projects {
    export namespace v2 {
        export interface GetProjectProperties {
            project?: outputs.projects.v2.Project;
        }

        export interface GetProjectsDefaultProperties {
            project?: outputs.projects.v2.Project;
        }

        export interface ListProjectsItems {
            links?: outputs.projects.v2.PageLinks;
            meta: outputs.projects.v2.MetaMeta;
            projects?: outputs.projects.v2.Project[];
        }

        export interface ListProjectsResourcesDefaultItems {
            links?: outputs.projects.v2.PageLinks;
            meta: outputs.projects.v2.MetaMeta;
            resources?: outputs.projects.v2.Resource[];
        }

        export interface ListProjectsResourcesItems {
            links?: outputs.projects.v2.PageLinks;
            meta: outputs.projects.v2.MetaMeta;
            resources?: outputs.projects.v2.Resource[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.projects.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Project {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the project was created.
             */
            createdAt?: string;
            /**
             * The description of the project. The maximum length is 255 characters.
             */
            description?: string;
            /**
             * The environment of the project's resources.
             */
            environment?: enums.projects.v2.ProjectBaseEnvironment;
            /**
             * The unique universal identifier of this project.
             */
            id?: string;
            /**
             * If true, all resources will be added to this project if no project is specified.
             */
            isDefault?: boolean;
            /**
             * The human-readable name for the project. The maximum length is 175 characters and the name must be unique.
             */
            name?: string;
            /**
             * The integer id of the project owner.
             */
            ownerId?: number;
            /**
             * The unique universal identifier of the project owner.
             */
            ownerUuid?: string;
            /**
             * The purpose of the project. The maximum length is 255 characters. It can
             * have one of the following values:
             *
             * - Just trying out DigitalOcean
             * - Class project / Educational purposes
             * - Website or blog
             * - Web Application
             * - Service or API
             * - Mobile Application
             * - Machine learning / AI / Data processing
             * - IoT
             * - Operational / Developer tooling
             *
             * If another value for purpose is specified, for example, "your custom purpose",
             * your purpose will be stored as `Other: your custom purpose`.
             */
            purpose?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the project was updated.
             */
            updatedAt?: string;
        }

        export interface Resource {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the project was created.
             */
            assignedAt?: string;
            /**
             * The links object contains the `self` object, which contains the resource relationship.
             */
            links?: outputs.projects.v2.ResourceLinksProperties;
            /**
             * The status of assigning and fetching the resources.
             */
            status?: enums.projects.v2.ResourceStatus;
            /**
             * The uniform resource name (URN) for the resource in the format do:resource_type:resource_id.
             */
            urn?: string;
        }

        /**
         * The links object contains the `self` object, which contains the resource relationship.
         */
        export interface ResourceLinksProperties {
            /**
             * A URI that can be used to retrieve the resource.
             */
            self?: string;
        }

    }
}

export namespace regions {
    export namespace v2 {
        export interface ListRegionsItems {
            links?: outputs.regions.v2.PageLinks;
            meta: outputs.regions.v2.MetaMeta;
            regions: outputs.regions.v2.Region[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.regions.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

    }
}

export namespace registry {
    export namespace v2 {
        export interface DockerCredentials {
            auths?: outputs.registry.v2.DockerCredentialsAuthsProperties;
        }

        export interface DockerCredentialsAuthsProperties {
            registryDigitaloceanCom?: outputs.registry.v2.DockerCredentialsAuthsPropertiesRegistryDigitaloceanComProperties;
        }

        export interface DockerCredentialsAuthsPropertiesRegistryDigitaloceanComProperties {
            /**
             * A base64 encoded string containing credentials for the container registry.
             */
            auth?: string;
        }

        export interface GarbageCollection {
            /**
             * The number of blobs deleted as a result of this garbage collection.
             */
            blobsDeleted?: number;
            /**
             * The time the garbage collection was created.
             */
            createdAt?: string;
            /**
             * The number of bytes freed as a result of this garbage collection.
             */
            freedBytes?: number;
            /**
             * The name of the container registry.
             */
            registryName?: string;
            /**
             * The current status of this garbage collection.
             */
            status?: enums.registry.v2.GarbageCollectionStatus;
            /**
             * The time the garbage collection was last updated.
             */
            updatedAt?: string;
            /**
             * A string specifying the UUID of the garbage collection.
             */
            uuid?: string;
        }

        export interface GetRegistryOptionProperties {
            options?: outputs.registry.v2.GetRegistryOptionPropertiesOptionsProperties;
        }

        export interface GetRegistryOptionPropertiesOptionsProperties {
            availableRegions?: string[];
            subscriptionTiers?: outputs.registry.v2.GetRegistryOptionPropertiesOptionsPropertiesSubscriptionTiersItem[];
        }

        export interface GetRegistryOptionPropertiesOptionsPropertiesSubscriptionTiersItem {
            /**
             * A boolean indicating whether the subscription tier supports additional storage above what is included in the base plan at an additional cost per GiB used.
             */
            allowStorageOverage?: boolean;
            /**
             * If your account is not eligible to use a certain subscription tier, this will include a list of reasons that prevent you from using the tier.
             */
            eligibilityReasons?: enums.registry.v2.SubscriptionTierExtendedEligibilityReasonsItem[];
            /**
             * A boolean indicating whether your account it eligible to use a certain subscription tier.
             */
            eligible?: boolean;
            /**
             * The amount of outbound data transfer included in the subscription tier in bytes.
             */
            includedBandwidthBytes?: number;
            /**
             * The number of repositories included in the subscription tier. `0` indicates that the subscription tier includes unlimited repositories.
             */
            includedRepositories?: number;
            /**
             * The amount of storage included in the subscription tier in bytes.
             */
            includedStorageBytes?: number;
            /**
             * The monthly cost of the subscription tier in cents.
             */
            monthlyPriceInCents?: number;
            /**
             * The name of the subscription tier.
             */
            name?: string;
            /**
             * The slug identifier of the subscription tier.
             */
            slug?: string;
            /**
             * The price paid in cents per GiB for additional storage beyond what is included in the subscription plan.
             */
            storageOveragePriceInCents?: number;
        }

        export interface GetRegistryProperties {
            registry?: outputs.registry.v2.Registry;
        }

        export interface GetRegistrySubscriptionProperties {
            subscription?: outputs.registry.v2.Subscription;
        }

        export interface ListRegistryGarbageCollectionsProperties {
            garbageCollections?: outputs.registry.v2.GarbageCollection[];
        }

        export interface ListRegistryRepositoriesItems {
            links?: outputs.registry.v2.PageLinks;
            meta: outputs.registry.v2.MetaMeta;
            repositories?: outputs.registry.v2.Repository[];
        }

        export interface ListRegistryRepositoriesV2Items {
            links?: outputs.registry.v2.PageLinks;
            meta: outputs.registry.v2.MetaMeta;
            repositories?: outputs.registry.v2.RepositoryV2[];
        }

        export interface ListRegistryRepositoryManifestsItems {
            links?: outputs.registry.v2.PageLinks;
            manifests?: outputs.registry.v2.RepositoryManifest[];
            meta: outputs.registry.v2.MetaMeta;
        }

        export interface ListRegistryRepositoryTagsItems {
            links?: outputs.registry.v2.PageLinks;
            meta: outputs.registry.v2.MetaMeta;
            tags?: outputs.registry.v2.RepositoryTag[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.registry.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Registry {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the registry was created.
             */
            createdAt?: string;
            /**
             * A globally unique name for the container registry. Must be lowercase and be composed only of numbers, letters and `-`, up to a limit of 63 characters.
             */
            name?: string;
            /**
             * Slug of the region where registry data is stored
             */
            region?: string;
            /**
             * The amount of storage used in the registry in bytes.
             */
            storageUsageBytes?: number;
            /**
             * The time at which the storage usage was updated. Storage usage is calculated asynchronously, and may not immediately reflect pushes to the registry.
             */
            storageUsageBytesUpdatedAt?: string;
            subscription?: outputs.registry.v2.RegistrySubscription;
        }

        export interface RegistrySubscription {
            /**
             * The time at which the subscription was created.
             */
            createdAt?: string;
            tier?: outputs.registry.v2.SubscriptionTierBase;
            /**
             * The time at which the subscription was last updated.
             */
            updatedAt?: string;
        }

        export interface Repository {
            latestTag?: outputs.registry.v2.RepositoryTag;
            /**
             * The name of the repository.
             */
            name?: string;
            /**
             * The name of the container registry.
             */
            registryName?: string;
            /**
             * The number of tags in the repository.
             */
            tagCount?: number;
        }

        export interface RepositoryBlob {
            /**
             * The compressed size of the blob in bytes.
             */
            compressedSizeBytes?: number;
            /**
             * The digest of the blob
             */
            digest?: string;
        }

        export interface RepositoryManifest {
            /**
             * All blobs associated with this manifest
             */
            blobs?: outputs.registry.v2.RepositoryBlob[];
            /**
             * The compressed size of the manifest in bytes.
             */
            compressedSizeBytes?: number;
            /**
             * The manifest digest
             */
            digest?: string;
            /**
             * The name of the container registry.
             */
            registryName?: string;
            /**
             * The name of the repository.
             */
            repository?: string;
            /**
             * The uncompressed size of the manifest in bytes (this size is calculated asynchronously so it may not be immediately available).
             */
            sizeBytes?: number;
            /**
             * All tags associated with this manifest
             */
            tags?: string[];
            /**
             * The time the manifest was last updated.
             */
            updatedAt?: string;
        }

        export interface RepositoryTag {
            /**
             * The compressed size of the tag in bytes.
             */
            compressedSizeBytes?: number;
            /**
             * The digest of the manifest associated with the tag.
             */
            manifestDigest?: string;
            /**
             * The name of the container registry.
             */
            registryName?: string;
            /**
             * The name of the repository.
             */
            repository?: string;
            /**
             * The uncompressed size of the tag in bytes (this size is calculated asynchronously so it may not be immediately available).
             */
            sizeBytes?: number;
            /**
             * The name of the tag.
             */
            tag?: string;
            /**
             * The time the tag was last updated.
             */
            updatedAt?: string;
        }

        export interface RepositoryV2 {
            latestManifest?: outputs.registry.v2.RepositoryManifest;
            /**
             * The number of manifests in the repository.
             */
            manifestCount?: number;
            /**
             * The name of the repository.
             */
            name?: string;
            /**
             * The name of the container registry.
             */
            registryName?: string;
            /**
             * The number of tags in the repository.
             */
            tagCount?: number;
        }

        export interface Subscription {
            /**
             * The time at which the subscription was created.
             */
            createdAt?: string;
            tier?: outputs.registry.v2.SubscriptionTierBase;
            /**
             * The time at which the subscription was last updated.
             */
            updatedAt?: string;
        }

        export interface SubscriptionTierBase {
            /**
             * A boolean indicating whether the subscription tier supports additional storage above what is included in the base plan at an additional cost per GiB used.
             */
            allowStorageOverage?: boolean;
            /**
             * The amount of outbound data transfer included in the subscription tier in bytes.
             */
            includedBandwidthBytes?: number;
            /**
             * The number of repositories included in the subscription tier. `0` indicates that the subscription tier includes unlimited repositories.
             */
            includedRepositories?: number;
            /**
             * The amount of storage included in the subscription tier in bytes.
             */
            includedStorageBytes?: number;
            /**
             * The monthly cost of the subscription tier in cents.
             */
            monthlyPriceInCents?: number;
            /**
             * The name of the subscription tier.
             */
            name?: string;
            /**
             * The slug identifier of the subscription tier.
             */
            slug?: string;
            /**
             * The price paid in cents per GiB for additional storage beyond what is included in the subscription plan.
             */
            storageOveragePriceInCents?: number;
        }

    }
}

export namespace reports {
    export namespace v2 {
        export interface NeighborIds {
            /**
             * An array of arrays. Each array will contain a set of Droplet IDs for Droplets that share a physical server.
             */
            neighborIds?: number[][];
        }

    }
}

export namespace reserved_ips {
    export namespace v2 {
        export interface Action {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            /**
             * The UUID of the project to which the reserved IP currently belongs.
             */
            projectId?: string;
            region?: outputs.reserved_ips.v2.Region;
            regionSlug?: outputs.reserved_ips.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.reserved_ips.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * actionProvideDefaults sets the appropriate defaults for Action
         */
        export function actionProvideDefaults(val: Action): Action {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        /**
         * The linked actions can be used to check the status of a Droplet's create event.
         */
        export interface ActionLink {
            /**
             * A URL that can be used to access the action.
             */
            href?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            /**
             * A string specifying the type of the related action.
             */
            rel?: string;
        }

        export interface ActionRegionSlug {
        }

        export interface Droplet {
            /**
             * An array of backup IDs of any backups that have been taken of the Droplet instance.  Droplet backups are enabled at the time of the instance creation.
             */
            backupIds: number[];
            /**
             * A time value given in ISO8601 combined date and time format that represents when the Droplet was created.
             */
            createdAt: string;
            /**
             * The size of the Droplet's disk in gigabytes.
             */
            disk: number;
            /**
             * An array of features enabled on this Droplet.
             */
            features: string[];
            /**
             * A unique identifier for each Droplet instance. This is automatically generated upon Droplet creation.
             */
            id: number;
            image: outputs.reserved_ips.v2.Image;
            /**
             * **Note**: All Droplets created after March 2017 use internal kernels by default.
             * These Droplets will have this attribute set to `null`.
             *
             * The current [kernel](https://www.digitalocean.com/docs/droplets/how-to/kernel/)
             * for Droplets with externally managed kernels. This will initially be set to
             * the kernel of the base image when the Droplet is created.
             */
            kernel?: outputs.reserved_ips.v2.Kernel;
            /**
             * A boolean value indicating whether the Droplet has been locked, preventing actions by users.
             */
            locked: boolean;
            /**
             * Memory of the Droplet in megabytes.
             */
            memory: number;
            /**
             * The human-readable name set for the Droplet instance.
             */
            name: string;
            /**
             * The details of the network that are configured for the Droplet instance.  This is an object that contains keys for IPv4 and IPv6.  The value of each of these is an array that contains objects describing an individual IP resource allocated to the Droplet.  These will define attributes like the IP address, netmask, and gateway of the specific network depending on the type of network it is.
             */
            networks: outputs.reserved_ips.v2.DropletNetworksProperties;
            /**
             * The details of the Droplet's backups feature, if backups are configured for the Droplet. This object contains keys for the start and end times of the window during which the backup will start.
             */
            nextBackupWindow: outputs.reserved_ips.v2.DropletNextBackupWindowProperties;
            region: outputs.reserved_ips.v2.Region;
            size: outputs.reserved_ips.v2.Size;
            /**
             * The unique slug identifier for the size of this Droplet.
             */
            sizeSlug: string;
            /**
             * An array of snapshot IDs of any snapshots created from the Droplet instance.
             */
            snapshotIds: number[];
            /**
             * A status string indicating the state of the Droplet instance. This may be "new", "active", "off", or "archive".
             */
            status: enums.reserved_ips.v2.DropletStatus;
            /**
             * An array of Tags the Droplet has been tagged with.
             */
            tags: string[];
            /**
             * The number of virtual CPUs.
             */
            vcpus: number;
            /**
             * A flat array including the unique identifier for each Block Storage volume attached to the Droplet.
             */
            volumeIds: string[];
            /**
             * A string specifying the UUID of the VPC to which the Droplet is assigned.
             */
            vpcUuid?: string;
        }
        /**
         * dropletProvideDefaults sets the appropriate defaults for Droplet
         */
        export function dropletProvideDefaults(val: Droplet): Droplet {
            return {
                ...val,
                size: outputs.reserved_ips.v2.sizeProvideDefaults(val.size),
            };
        }

        /**
         * The details of the network that are configured for the Droplet instance.  This is an object that contains keys for IPv4 and IPv6.  The value of each of these is an array that contains objects describing an individual IP resource allocated to the Droplet.  These will define attributes like the IP address, netmask, and gateway of the specific network depending on the type of network it is.
         */
        export interface DropletNetworksProperties {
            v4?: outputs.reserved_ips.v2.NetworkV4[];
            v6?: outputs.reserved_ips.v2.NetworkV6[];
        }

        /**
         * The details of the Droplet's backups feature, if backups are configured for the Droplet. This object contains keys for the start and end times of the window during which the backup will start.
         */
        export interface DropletNextBackupWindowProperties {
            /**
             * A time value given in ISO8601 combined date and time format specifying the end of the Droplet's backup window.
             */
            end?: string;
            /**
             * A time value given in ISO8601 combined date and time format specifying the start of the Droplet's backup window.
             */
            start?: string;
        }

        export interface GetReservedIPProperties {
            reservedIp?: outputs.reserved_ips.v2.ReservedIp;
        }
        /**
         * getReservedIPPropertiesProvideDefaults sets the appropriate defaults for GetReservedIPProperties
         */
        export function getReservedIPPropertiesProvideDefaults(val: GetReservedIPProperties): GetReservedIPProperties {
            return {
                ...val,
                reservedIp: (val.reservedIp ? outputs.reserved_ips.v2.reservedIpProvideDefaults(val.reservedIp) : undefined),
            };
        }

        export interface GetReservedIPsActionProperties {
            action?: outputs.reserved_ips.v2.GetReservedIPsActionPropertiesAction;
        }
        /**
         * getReservedIPsActionPropertiesProvideDefaults sets the appropriate defaults for GetReservedIPsActionProperties
         */
        export function getReservedIPsActionPropertiesProvideDefaults(val: GetReservedIPsActionProperties): GetReservedIPsActionProperties {
            return {
                ...val,
                action: (val.action ? outputs.reserved_ips.v2.getReservedIPsActionPropertiesActionProvideDefaults(val.action) : undefined),
            };
        }

        export interface GetReservedIPsActionPropertiesAction {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            /**
             * The UUID of the project to which the reserved IP currently belongs.
             */
            projectId?: string;
            region?: outputs.reserved_ips.v2.Region;
            regionSlug?: outputs.reserved_ips.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.reserved_ips.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * getReservedIPsActionPropertiesActionProvideDefaults sets the appropriate defaults for GetReservedIPsActionPropertiesAction
         */
        export function getReservedIPsActionPropertiesActionProvideDefaults(val: GetReservedIPsActionPropertiesAction): GetReservedIPsActionPropertiesAction {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        export interface Image {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the image was created.
             */
            createdAt?: string;
            /**
             * An optional free-form text field to describe an image.
             */
            description?: string;
            /**
             * The name of a custom image's distribution. Currently, the valid values are  `Arch Linux`, `CentOS`, `CoreOS`, `Debian`, `Fedora`, `Fedora Atomic`,  `FreeBSD`, `Gentoo`, `openSUSE`, `RancherOS`, `Rocky Linux`, `Ubuntu`, and `Unknown`.  Any other value will be accepted but ignored, and `Unknown` will be used in its place.
             */
            distribution?: enums.reserved_ips.v2.ImageDistribution;
            /**
             * A string containing information about errors that may occur when importing
             *  a custom image.
             */
            errorMessage?: string;
            /**
             * A unique number that can be used to identify and reference a specific image.
             */
            id?: number;
            /**
             * The minimum disk size in GB required for a Droplet to use this image.
             */
            minDiskSize?: number;
            /**
             * The display name that has been given to an image.  This is what is shown in the control panel and is generally a descriptive title for the image in question.
             */
            name?: string;
            /**
             * This is a boolean value that indicates whether the image in question is public or not. An image that is public is available to all accounts. A non-public image is only accessible from your account.
             */
            public?: boolean;
            /**
             * This attribute is an array of the regions that the image is available in. The regions are represented by their identifying slug values.
             */
            regions?: enums.reserved_ips.v2.ImageRegionsItem[];
            /**
             * The size of the image in gigabytes.
             */
            sizeGigabytes?: number;
            /**
             * A uniquely identifying string that is associated with each of the DigitalOcean-provided public images. These can be used to reference a public image as an alternative to the numeric id.
             */
            slug?: string;
            /**
             * A status string indicating the state of a custom image. This may be `NEW`,
             *  `available`, `pending`, `deleted`, or `retired`.
             */
            status?: enums.reserved_ips.v2.ImageStatus;
            /**
             * A flat array of tag names as strings to be applied to the resource. Tag names may be for either existing or new tags.
             */
            tags?: string[];
            /**
             * Describes the kind of image. It may be one of `base`, `snapshot`, `backup`, `custom`, or `admin`. Respectively, this specifies whether an image is a DigitalOcean base OS image, user-generated Droplet snapshot, automatically created Droplet backup, user-provided virtual machine image, or an image used for DigitalOcean managed resources (e.g. DOKS worker nodes).
             */
            type?: enums.reserved_ips.v2.ImageType;
        }

        /**
         * **Note**: All Droplets created after March 2017 use internal kernels by default.
         * These Droplets will have this attribute set to `null`.
         *
         * The current [kernel](https://www.digitalocean.com/docs/droplets/how-to/kernel/)
         * for Droplets with externally managed kernels. This will initially be set to
         * the kernel of the base image when the Droplet is created.
         */
        export interface Kernel {
            /**
             * A unique number used to identify and reference a specific kernel.
             */
            id?: number;
            /**
             * The display name of the kernel. This is shown in the web UI and is generally a descriptive title for the kernel in question.
             */
            name?: string;
            /**
             * A standard kernel version string representing the version, patch, and release information.
             */
            version?: string;
        }

        export interface LinksProperties {
            actions?: outputs.reserved_ips.v2.ActionLink[];
            droplets?: outputs.reserved_ips.v2.ActionLink[];
        }

        export interface ListReservedIPsActionsItems {
            actions?: outputs.reserved_ips.v2.Action[];
            links?: outputs.reserved_ips.v2.PageLinks;
            meta: outputs.reserved_ips.v2.MetaMeta;
        }

        export interface ListReservedIPsItems {
            links?: outputs.reserved_ips.v2.PageLinks;
            meta: outputs.reserved_ips.v2.MetaMeta;
            reservedIps?: outputs.reserved_ips.v2.ReservedIp[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface NetworkV4 {
            /**
             * The gateway of the specified IPv4 network interface.
             *
             * For private interfaces, a gateway is not provided. This is denoted by
             * returning `nil` as its value.
             */
            gateway?: string;
            /**
             * The IP address of the IPv4 network interface.
             */
            ipAddress?: string;
            /**
             * The netmask of the IPv4 network interface.
             */
            netmask?: string;
            /**
             * The type of the IPv4 network interface.
             */
            type?: enums.reserved_ips.v2.NetworkV4Type;
        }

        export interface NetworkV6 {
            /**
             * The gateway of the specified IPv6 network interface.
             */
            gateway?: string;
            /**
             * The IP address of the IPv6 network interface.
             */
            ipAddress?: string;
            /**
             * The netmask of the IPv6 network interface.
             */
            netmask?: number;
            /**
             * The type of the IPv6 network interface.
             *
             * **Note**: IPv6 private  networking is not currently supported.
             */
            type?: enums.reserved_ips.v2.NetworkV6Type;
        }

        export interface PageLinks {
            pages?: outputs.reserved_ips.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface ReservedIp {
            /**
             * The Droplet that the reserved IP has been assigned to. When you query a reserved IP, if it is assigned to a Droplet, the entire Droplet object will be returned. If it is not assigned, the value will be null.
             */
            droplet?: outputs.reserved_ips.v2.Droplet;
            /**
             * The public IP address of the reserved IP. It also serves as its identifier.
             */
            ip?: string;
            /**
             * A boolean value indicating whether or not the reserved IP has pending actions preventing new ones from being submitted.
             */
            locked?: boolean;
            /**
             * The UUID of the project to which the reserved IP currently belongs.
             */
            projectId?: string;
            region?: outputs.reserved_ips.v2.ReservedIpRegion;
        }
        /**
         * reservedIpProvideDefaults sets the appropriate defaults for ReservedIp
         */
        export function reservedIpProvideDefaults(val: ReservedIp): ReservedIp {
            return {
                ...val,
                droplet: (val.droplet ? outputs.reserved_ips.v2.dropletProvideDefaults(val.droplet) : undefined),
            };
        }

        export interface ReservedIpRegion {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface Size {
            /**
             * This is a boolean value that represents whether new Droplets can be created with this size.
             */
            available: boolean;
            /**
             * A string describing the class of Droplets created from this size. For example: Basic, General Purpose, CPU-Optimized, Memory-Optimized, or Storage-Optimized.
             */
            description: string;
            /**
             * The amount of disk space set aside for Droplets of this size. The value is represented in gigabytes.
             */
            disk: number;
            /**
             * The amount of RAM allocated to Droplets created of this size. The value is represented in megabytes.
             */
            memory: number;
            /**
             * This describes the price of the Droplet size as measured hourly. The value is measured in US dollars.
             */
            priceHourly: number;
            /**
             * This attribute describes the monthly cost of this Droplet size if the Droplet is kept for an entire month. The value is measured in US dollars.
             */
            priceMonthly: number;
            /**
             * An array containing the region slugs where this size is available for Droplet creates.
             */
            regions: string[];
            /**
             * A human-readable string that is used to uniquely identify each size.
             */
            slug: string;
            /**
             * The amount of transfer bandwidth that is available for Droplets created in this size. This only counts traffic on the public interface. The value is given in terabytes.
             */
            transfer: number;
            /**
             * The integer of number CPUs allocated to Droplets of this size.
             */
            vcpus: number;
        }
        /**
         * sizeProvideDefaults sets the appropriate defaults for Size
         */
        export function sizeProvideDefaults(val: Size): Size {
            return {
                ...val,
                available: (val.available) ?? true,
            };
        }

    }
}

export namespace sizes {
    export namespace v2 {
        export interface ListSizesItems {
            links?: outputs.sizes.v2.PageLinks;
            meta: outputs.sizes.v2.MetaMeta;
            sizes: outputs.sizes.v2.Size[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.sizes.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Size {
            /**
             * This is a boolean value that represents whether new Droplets can be created with this size.
             */
            available: boolean;
            /**
             * A string describing the class of Droplets created from this size. For example: Basic, General Purpose, CPU-Optimized, Memory-Optimized, or Storage-Optimized.
             */
            description: string;
            /**
             * The amount of disk space set aside for Droplets of this size. The value is represented in gigabytes.
             */
            disk: number;
            /**
             * The amount of RAM allocated to Droplets created of this size. The value is represented in megabytes.
             */
            memory: number;
            /**
             * This describes the price of the Droplet size as measured hourly. The value is measured in US dollars.
             */
            priceHourly: number;
            /**
             * This attribute describes the monthly cost of this Droplet size if the Droplet is kept for an entire month. The value is measured in US dollars.
             */
            priceMonthly: number;
            /**
             * An array containing the region slugs where this size is available for Droplet creates.
             */
            regions: string[];
            /**
             * A human-readable string that is used to uniquely identify each size.
             */
            slug: string;
            /**
             * The amount of transfer bandwidth that is available for Droplets created in this size. This only counts traffic on the public interface. The value is given in terabytes.
             */
            transfer: number;
            /**
             * The integer of number CPUs allocated to Droplets of this size.
             */
            vcpus: number;
        }
        /**
         * sizeProvideDefaults sets the appropriate defaults for Size
         */
        export function sizeProvideDefaults(val: Size): Size {
            return {
                ...val,
                available: (val.available) ?? true,
            };
        }

    }
}

export namespace snapshots {
    export namespace v2 {
        export interface GetSnapshotProperties {
            snapshot?: outputs.snapshots.v2.Snapshots;
        }

        export interface ListSnapshotsItems {
            links?: outputs.snapshots.v2.PageLinks;
            meta: outputs.snapshots.v2.MetaMeta;
            snapshots?: outputs.snapshots.v2.Snapshots[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.snapshots.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Snapshots {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the snapshot was created.
             */
            createdAt: string;
            /**
             * The minimum size in GB required for a volume or Droplet to use this snapshot.
             */
            minDiskSize: number;
            /**
             * A human-readable name for the snapshot.
             */
            name: string;
            /**
             * An array of the regions that the snapshot is available in. The regions are represented by their identifying slug values.
             */
            regions: string[];
            /**
             * The unique identifier for the resource that the snapshot originated from.
             */
            resourceId: string;
            /**
             * The type of resource that the snapshot originated from.
             */
            resourceType: enums.snapshots.v2.SnapshotsPropertiesResourceType;
            /**
             * The billable size of the snapshot in gigabytes.
             */
            sizeGigabytes: number;
            /**
             * An array of Tags the snapshot has been tagged with.
             */
            tags: string[];
        }

    }
}

export namespace tags {
    export namespace v2 {
        export interface GetTagProperties {
            /**
             * A tag is a label that can be applied to a resource (currently Droplets, Images, Volumes, Volume Snapshots, and Database clusters) in order to better organize or facilitate the lookups and actions on it.
             * Tags have two attributes: a user defined `name` attribute and an embedded `resources` attribute with information about resources that have been tagged.
             */
            tag?: outputs.tags.v2.Tags;
        }

        export interface ListTagsItems {
            links?: outputs.tags.v2.PageLinks;
            meta: outputs.tags.v2.MetaMeta;
            tags?: outputs.tags.v2.Tags[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.tags.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        /**
         * An embedded object containing key value pairs of resource type and resource statistics. It also includes a count of the total number of resources tagged with the current tag as well as a `last_tagged_uri` attribute set to the last resource tagged with the current tag.
         */
        export interface Resources {
            /**
             * The number of tagged objects for this type of resource.
             */
            count?: number;
            /**
             * The URI for the last tagged object for this type of resource.
             */
            lastTaggedUri?: string;
        }

        export interface ResourcesItemProperties {
            /**
             * The identifier of a resource.
             */
            resourceId?: string;
            /**
             * The type of the resource.
             */
            resourceType?: enums.tags.v2.TagsAssignResourcesResourcesItemPropertiesResourceType;
        }

        /**
         * A tag is a label that can be applied to a resource (currently Droplets, Images, Volumes, Volume Snapshots, and Database clusters) in order to better organize or facilitate the lookups and actions on it.
         * Tags have two attributes: a user defined `name` attribute and an embedded `resources` attribute with information about resources that have been tagged.
         */
        export interface Tags {
            /**
             * The name of the tag. Tags may contain letters, numbers, colons, dashes, and underscores.
             * There is a limit of 255 characters per tag.
             *
             * **Note:** Tag names are case stable, which means the capitalization you use when you first create a tag is canonical.
             *
             * When working with tags in the API, you must use the tag's canonical capitalization. For example, if you create a tag named "PROD", the URL to add that tag to a resource would be `https://api.digitalocean.com/v2/tags/PROD/resources` (not `/v2/tags/prod/resources`).
             *
             * Tagged resources in the control panel will always display the canonical capitalization. For example, if you create a tag named "PROD", you can tag resources in the control panel by entering "prod". The tag will still display with its canonical capitalization, "PROD".
             */
            name?: string;
            /**
             * An embedded object containing key value pairs of resource type and resource statistics. It also includes a count of the total number of resources tagged with the current tag as well as a `last_tagged_uri` attribute set to the last resource tagged with the current tag.
             */
            resources?: outputs.tags.v2.TagsResources;
        }

        /**
         * An embedded object containing key value pairs of resource type and resource statistics. It also includes a count of the total number of resources tagged with the current tag as well as a `last_tagged_uri` attribute set to the last resource tagged with the current tag.
         */
        export interface TagsResources {
            /**
             * The number of tagged objects for this type of resource.
             */
            count?: number;
            /**
             * The URI for the last tagged object for this type of resource.
             */
            lastTaggedUri?: string;
        }

    }
}

export namespace uptime {
    export namespace v2 {
        export interface Alert {
            /**
             * The comparison operator used against the alert's threshold.
             */
            comparison?: enums.uptime.v2.AlertUpdatableComparison;
            /**
             * A unique ID that can be used to identify and reference the alert.
             */
            id?: string;
            /**
             * A human-friendly display name.
             */
            name?: string;
            /**
             * The notification settings for a trigger alert.
             */
            notifications?: outputs.uptime.v2.Notification;
            /**
             * Period of time the threshold must be exceeded to trigger the alert.
             */
            period?: enums.uptime.v2.AlertUpdatablePeriod;
            /**
             * The threshold at which the alert will enter a trigger state. The specific threshold is dependent on the alert type.
             */
            threshold?: number;
            /**
             * The type of alert.
             */
            type?: enums.uptime.v2.AlertUpdatableType;
        }

        export interface Check {
            /**
             * A boolean value indicating whether the check is enabled/disabled.
             */
            enabled?: boolean;
            /**
             * A unique ID that can be used to identify and reference the check.
             */
            id?: string;
            /**
             * A human-friendly display name.
             */
            name?: string;
            /**
             * An array containing the selected regions to perform healthchecks from.
             */
            regions?: enums.uptime.v2.CheckUpdatableRegionsItem[];
            /**
             * The endpoint to perform healthchecks on.
             */
            target?: string;
            /**
             * The type of health check to perform.
             */
            type?: enums.uptime.v2.CheckUpdatableType;
        }
        /**
         * checkProvideDefaults sets the appropriate defaults for Check
         */
        export function checkProvideDefaults(val: Check): Check {
            return {
                ...val,
                enabled: (val.enabled) ?? true,
            };
        }

        export interface GetUptimeAlertProperties {
            alert?: outputs.uptime.v2.Alert;
        }

        export interface GetUptimeCheckProperties {
            check?: outputs.uptime.v2.Check;
        }
        /**
         * getUptimeCheckPropertiesProvideDefaults sets the appropriate defaults for GetUptimeCheckProperties
         */
        export function getUptimeCheckPropertiesProvideDefaults(val: GetUptimeCheckProperties): GetUptimeCheckProperties {
            return {
                ...val,
                check: (val.check ? outputs.uptime.v2.checkProvideDefaults(val.check) : undefined),
            };
        }

        export interface GetUptimeCheckStateProperties {
            state?: outputs.uptime.v2.State;
        }

        export interface ListUptimeAlertsItems {
            alerts?: outputs.uptime.v2.Alert[];
            links?: outputs.uptime.v2.PageLinks;
            meta: outputs.uptime.v2.MetaMeta;
        }

        export interface ListUptimeChecksItems {
            checks?: outputs.uptime.v2.Check[];
            links?: outputs.uptime.v2.PageLinks;
            meta: outputs.uptime.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        /**
         * The notification settings for a trigger alert.
         */
        export interface Notification {
            /**
             * An email to notify on an alert trigger.
             */
            email: string[];
            /**
             * Slack integration details.
             */
            slack: outputs.uptime.v2.NotificationSlackItemProperties[];
        }

        export interface NotificationSlackItemProperties {
            /**
             * Slack channel to notify of an alert trigger.
             */
            channel: string;
            /**
             * Slack Webhook URL.
             */
            url: string;
        }

        export interface PageLinks {
            pages?: outputs.uptime.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface PreviousOutage {
            durationSeconds?: number;
            endedAt?: string;
            region?: string;
            startedAt?: string;
        }

        export interface RegionState {
            status?: enums.uptime.v2.RegionStateStatus;
            statusChangedAt?: string;
            thirtyDayUptimePercentage?: number;
        }

        /**
         * A map of region to regional state
         */
        export interface RegionalState {
            euWest?: outputs.uptime.v2.RegionState;
            usEast?: outputs.uptime.v2.RegionState;
        }

        export interface State {
            previousOutage?: outputs.uptime.v2.PreviousOutage;
            /**
             * A map of region to regional state
             */
            regions?: outputs.uptime.v2.RegionalState;
        }

    }
}

export namespace volumes {
    export namespace v2 {
        export interface ActionRegionSlug {
        }

        export interface GetVolumeActionProperties {
            action?: outputs.volumes.v2.VolumeAction;
        }
        /**
         * getVolumeActionPropertiesProvideDefaults sets the appropriate defaults for GetVolumeActionProperties
         */
        export function getVolumeActionPropertiesProvideDefaults(val: GetVolumeActionProperties): GetVolumeActionProperties {
            return {
                ...val,
                action: (val.action ? outputs.volumes.v2.volumeActionProvideDefaults(val.action) : undefined),
            };
        }

        export interface GetVolumeProperties {
            volume?: outputs.volumes.v2.VolumeFull;
        }

        export interface GetVolumeSnapshotsByIdProperties {
            snapshot?: outputs.volumes.v2.Snapshots;
        }

        export interface ListVolumeActionsItems {
            actions?: outputs.volumes.v2.VolumeAction[];
            links?: outputs.volumes.v2.PageLinks;
            meta: outputs.volumes.v2.MetaMeta;
        }

        export interface ListVolumeSnapshotsItems {
            links?: outputs.volumes.v2.PageLinks;
            meta: outputs.volumes.v2.MetaMeta;
            snapshots?: outputs.volumes.v2.Snapshots[];
        }

        export interface ListVolumesItems {
            links?: outputs.volumes.v2.PageLinks;
            meta: outputs.volumes.v2.MetaMeta;
            /**
             * Array of volumes.
             */
            volumes: outputs.volumes.v2.VolumeFull[];
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.volumes.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Region {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

        export interface Snapshots {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the snapshot was created.
             */
            createdAt: string;
            /**
             * The minimum size in GB required for a volume or Droplet to use this snapshot.
             */
            minDiskSize: number;
            /**
             * A human-readable name for the snapshot.
             */
            name: string;
            /**
             * An array of the regions that the snapshot is available in. The regions are represented by their identifying slug values.
             */
            regions: string[];
            /**
             * The unique identifier for the resource that the snapshot originated from.
             */
            resourceId: string;
            /**
             * The type of resource that the snapshot originated from.
             */
            resourceType: enums.volumes.v2.SnapshotsPropertiesResourceType;
            /**
             * The billable size of the snapshot in gigabytes.
             */
            sizeGigabytes: number;
            /**
             * An array of Tags the snapshot has been tagged with.
             */
            tags: string[];
        }

        export interface VolumeAction {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was completed.
             */
            completedAt?: string;
            /**
             * A unique numeric ID that can be used to identify and reference an action.
             */
            id?: number;
            region?: outputs.volumes.v2.Region;
            regionSlug?: outputs.volumes.v2.ActionRegionSlug;
            /**
             * A unique identifier for the resource that the action is associated with.
             */
            resourceId?: number;
            /**
             * The type of resource that the action is associated with.
             */
            resourceType?: string;
            /**
             * A time value given in ISO8601 combined date and time format that represents when the action was initiated.
             */
            startedAt?: string;
            /**
             * The current status of the action. This can be "in-progress", "completed", or "errored".
             */
            status?: enums.volumes.v2.ActionStatus;
            /**
             * This is the type of action that the object represents. For example, this could be "transfer" to represent the state of an image transfer action.
             */
            type?: string;
        }
        /**
         * volumeActionProvideDefaults sets the appropriate defaults for VolumeAction
         */
        export function volumeActionProvideDefaults(val: VolumeAction): VolumeAction {
            return {
                ...val,
                status: (val.status) ?? "in-progress",
            };
        }

        export interface VolumeFull {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the block storage volume was created.
             */
            createdAt?: string;
            /**
             * An optional free-form text field to describe a block storage volume.
             */
            description?: string;
            /**
             * An array containing the IDs of the Droplets the volume is attached to. Note that at this time, a volume can only be attached to a single Droplet.
             */
            dropletIds?: number[];
            /**
             * The label currently applied to the filesystem.
             */
            filesystemLabel?: string;
            /**
             * The type of filesystem currently in-use on the volume.
             */
            filesystemType?: string;
            /**
             * The unique identifier for the block storage volume.
             */
            id?: string;
            /**
             * A human-readable name for the block storage volume. Must be lowercase and be composed only of numbers, letters and "-", up to a limit of 64 characters. The name must begin with a letter.
             */
            name?: string;
            region?: outputs.volumes.v2.VolumeFullPropertiesRegion;
            /**
             * The size of the block storage volume in GiB (1024^3). This field does not apply  when creating a volume from a snapshot.
             */
            sizeGigabytes?: number;
            /**
             * A flat array of tag names as strings to be applied to the resource. Tag names may be for either existing or new tags.
             */
            tags?: string[];
        }

        export interface VolumeFullPropertiesRegion {
            /**
             * This is a boolean value that represents whether new Droplets can be created in this region.
             */
            available: boolean;
            /**
             * This attribute is set to an array which contains features available in this region
             */
            features: string[];
            /**
             * The display name of the region.  This will be a full name that is used in the control panel and other interfaces.
             */
            name: string;
            /**
             * This attribute is set to an array which contains the identifying slugs for the sizes available in this region.
             */
            sizes: string[];
            /**
             * A human-readable string that is used as a unique identifier for each region.
             */
            slug: string;
        }

    }
}

export namespace vpcs {
    export namespace v2 {
        export interface GetVpcProperties {
            vpc?: outputs.vpcs.v2.Vpc;
        }

        export interface ListVpcsItems {
            links?: outputs.vpcs.v2.PageLinks;
            meta: outputs.vpcs.v2.MetaMeta;
            vpcs?: outputs.vpcs.v2.Vpc[];
        }

        export interface ListVpcsMembersItems {
            links?: outputs.vpcs.v2.PageLinks;
            members?: outputs.vpcs.v2.VpcMember[];
            meta: outputs.vpcs.v2.MetaMeta;
        }

        export interface MetaMeta {
            /**
             * Number of objects returned by the request.
             */
            total?: number;
        }

        export interface PageLinks {
            pages?: outputs.vpcs.v2.PageLinksPagesProperties;
        }

        export interface PageLinksPagesProperties {
            first?: string;
            last?: string;
            next?: string;
            prev?: string;
        }

        export interface Vpc {
            /**
             * A time value given in ISO8601 combined date and time format.
             */
            createdAt?: string;
            /**
             * A boolean value indicating whether or not the VPC is the default network for the region. All applicable resources are placed into the default VPC network unless otherwise specified during their creation. The `default` field cannot be unset from `true`. If you want to set a new default VPC network, update the `default` field of another VPC network in the same region. The previous network's `default` field will be set to `false` when a new default VPC has been defined.
             */
            default?: boolean;
            /**
             * A free-form text field for describing the VPC's purpose. It may be a maximum of 255 characters.
             */
            description?: string;
            /**
             * A unique ID that can be used to identify and reference the VPC.
             */
            id?: string;
            /**
             * The range of IP addresses in the VPC in CIDR notation. Network ranges cannot overlap with other networks in the same account and must be in range of private addresses as defined in RFC1918. It may not be smaller than `/28` nor larger than `/16`. If no IP range is specified, a `/20` network range is generated that won't conflict with other VPC networks in your account.
             */
            ipRange?: string;
            /**
             * The name of the VPC. Must be unique and may only contain alphanumeric characters, dashes, and periods.
             */
            name?: string;
            /**
             * The slug identifier for the region where the VPC will be created.
             */
            region?: string;
            /**
             * The uniform resource name (URN) for the resource in the format do:resource_type:resource_id.
             */
            urn?: string;
        }

        export interface VpcMember {
            /**
             * A time value given in ISO8601 combined date and time format that represents when the resource was created.
             */
            createdAt?: string;
            /**
             * The name of the resource.
             */
            name?: string;
            /**
             * The uniform resource name (URN) for the resource in the format do:resource_type:resource_id.
             */
            urn?: string;
        }

    }
}
